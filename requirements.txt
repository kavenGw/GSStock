flask>=3.0.0
flask-sqlalchemy>=3.1.0
python-dotenv>=1.0.0
pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
# rapidocr-onnxruntime: 仅 Windows 需要（OCR 持仓截图识别），Linux 部署不需要
rapidocr-onnxruntime>=1.3.0,<2.0.0; sys_platform == "win32"
yfinance>=0.2.0
akshare>=1.10.0
exchange-calendars>=4.5.0
httpx[socks]>=0.27.0
apscheduler>=3.10.0
pyyaml>=6.0

# 生产部署（Linux）
gunicorn>=22.0.0

# 新闻兴趣模块
crawl4ai>=0.8.0
feedparser>=6.0.0
beautifulsoup4>=4.12.0
playwright>=1.40.0

# GPU 加速（根据硬件选择一个安装）:
# NVIDIA GPU: pip install onnxruntime-gpu>=1.19.0
# Windows DirectML: pip install onnxruntime-directml>=1.19.0
# CPU-only: 已包含在 rapidocr-onnxruntime 中，无需额外安装

# AI 走势预测（可选，未安装时自动跳过）
# CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
# CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu124
# torch>=2.0.0
