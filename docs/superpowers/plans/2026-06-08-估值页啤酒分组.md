# 估值页啤酒分组 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 估值汇总页把数据库分类「啤酒」抬成独立顶级分组，覆盖其 sector 归属；其余股票分组逻辑零变化。

**Architecture:** 在 `app/routes/valuations.py` 接入 `Category`/`StockCategory` 表，构建 `{stock_code: category_name}` 映射；分组时白名单分类（`CARVE_OUT_CATEGORIES`，首期 `{'啤酒'}`）命中则用分类名作 bucket key，否则走原 sector 逻辑。模板零改动（已是数据驱动）。3 只啤酒股的分类归属通过一次性 DB 写入设置。

**Tech Stack:** Flask + SQLAlchemy + SQLite + pytest；前端 Jinja2 + 原生 JS（不改动）。

参考 spec：`docs/superpowers/specs/2026-06-08-估值页啤酒分组-design.md`

---

## File Structure

- **Modify** `app/routes/valuations.py`
  - 新增常量 `CARVE_OUT_CATEGORIES = {'啤酒'}`
  - 新增函数 `load_category_map() -> dict[str, str]`（DB 读，app-context/异常守卫，失败返回 `{}`）
  - 改 `_enrich(rows, prices, cat_map=None)`：给每行附 `category`
  - 改 `group_by_sector(rows)`：白名单分类 carve-out
  - 改 `index()`：串起 `load_category_map()`
- **Modify** `tests/test_valuations.py`：新增 6 个测试
- **一次性脚本（不入库）** `scripts/_setup_beer_category.py`：建「啤酒」分类 + 改挂 3 只票，跑完即删
- **模板** `app/templates/valuations.html`：**不改动**

约束提醒（来自项目 rules）：
- 测试命令 env 必须在 `rtk` 之前：`PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest ...`
- `git add` 与 `git commit` 放进**同一条** Bash 命令链
- 一次性脚本顶部需 `sys.path.insert(0, ...)`（本计划用直连 sqlite3，无需 import app）

---

## Task 1: load_category_map（分类映射加载，异常降级）

**Files:**
- Modify: `app/routes/valuations.py`（在 `compute_margin` 之后、`load_valuations` 之前新增函数）
- Test: `tests/test_valuations.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_valuations.py` 末尾追加：

```python
def test_load_category_map_degrades_to_empty_on_error(monkeypatch):
    from app.routes.valuations import load_category_map

    class _BoomQuery:
        @staticmethod
        def all():
            raise RuntimeError('db down')

    class _BoomModel:
        query = _BoomQuery()

    monkeypatch.setattr('app.models.category.StockCategory', _BoomModel)
    assert load_category_map() == {}


def test_load_category_map_returns_dict(app_client):
    from app.routes.valuations import load_category_map
    with app_client.application.app_context():
        m = load_category_map()
    assert isinstance(m, dict)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_load_category_map_degrades_to_empty_on_error -v`
Expected: FAIL，`ImportError: cannot import name 'load_category_map'`

- [ ] **Step 3: 写最小实现**

在 `app/routes/valuations.py` 的 `compute_margin` 函数之后新增：

```python
def load_category_map() -> dict[str, str]:
    """返回 {stock_code: category_name}，来自 StockCategory join Category。
    app-context / 异常守卫：任何失败返回 {}（与取价失败降级同款，不让分类问题打挂整页）。"""
    try:
        from app.models.category import StockCategory
        return {
            sc.stock_code: sc.category.name
            for sc in StockCategory.query.all()
            if sc.category is not None
        }
    except Exception as e:
        logger.warning(f'[估值页] 取分类失败，降级无分类: {type(e).__name__}: {e}', exc_info=True)
        return {}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_load_category_map_degrades_to_empty_on_error tests/test_valuations.py::test_load_category_map_returns_dict -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat(valuations): 新增 load_category_map 加载股票分类映射"
```

---

## Task 2: _enrich 附 category 字段

**Files:**
- Modify: `app/routes/valuations.py:57-70`（`_enrich`）
- Test: `tests/test_valuations.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_enrich_attaches_category_from_cat_map():
    from app.routes.valuations import _enrich
    out = _enrich([{'stock_code': '600132', 'base': 10.0}], {'600132': {'price': 5.0}}, {'600132': '啤酒'})
    assert out[0]['category'] == '啤酒'


def test_enrich_category_none_without_map():
    from app.routes.valuations import _enrich
    out = _enrich([{'stock_code': 'x', 'base': 1.0}], {})
    assert out[0]['category'] is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_enrich_attaches_category_from_cat_map -v`
Expected: FAIL，`TypeError: _enrich() takes 2 positional arguments but 3 were given`

- [ ] **Step 3: 写最小实现**

把 `app/routes/valuations.py` 的 `_enrich` 整体替换为：

```python
def _enrich(rows: list[dict], prices: dict, cat_map: Optional[dict] = None) -> list[dict]:
    cat_map = cat_map or {}
    out = []
    for r in rows:
        data = prices.get(r['stock_code']) or {}
        price = _extract_price(data)
        out.append({
            **r,
            'category': cat_map.get(r['stock_code']),
            'current_price': price,
            'margin_bear': compute_margin(r.get('bear'), price),
            'margin_base': compute_margin(r.get('base'), price),
            'margin_bull': compute_margin(r.get('bull'), price),
        })
    out.sort(key=lambda x: (x['margin_base'] is None, -(x['margin_base'] or 0)))
    return out
```

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_enrich_attaches_category_from_cat_map tests/test_valuations.py::test_enrich_category_none_without_map -v`
Expected: PASS（2 passed）

- [ ] **Step 5: 提交**

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat(valuations): _enrich 支持附加 category 字段"
```

---

## Task 3: group_by_sector 白名单分类 carve-out

**Files:**
- Modify: `app/routes/valuations.py`（在 `SECTOR_LABELS` 后新增常量；替换 `group_by_sector`）
- Test: `tests/test_valuations.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_group_by_sector_carves_out_whitelisted_category():
    from app.routes.valuations import group_by_sector
    rows = [
        {'stock_code': '600600', 'sector': 'other', 'category': '啤酒', 'margin_base': 0.1},
        {'stock_code': '600132', 'sector': 'consumer', 'category': '啤酒', 'margin_base': 0.3},
        {'stock_code': '000001', 'sector': 'consumer', 'category': None, 'margin_base': 0.2},
    ]
    groups = {g['sector']: g for g in group_by_sector(rows)}
    assert '啤酒' in groups
    beer = groups['啤酒']
    assert beer['label'] == '啤酒'
    assert beer['count'] == 2
    assert [r['stock_code'] for r in beer['rows']] == ['600132', '600600']  # base 边际降序
    assert [r['stock_code'] for r in groups['consumer']['rows']] == ['000001']


def test_group_by_sector_non_whitelisted_category_uses_sector():
    from app.routes.valuations import group_by_sector
    [grp] = group_by_sector([{'stock_code': 'x', 'sector': 'consumer', 'category': '赛事消费', 'margin_base': 0.1}])
    assert grp['sector'] == 'consumer'
    assert grp['label'] == '消费'


def test_group_by_sector_carveout_row_gets_category_label():
    from app.routes.valuations import group_by_sector
    [grp] = group_by_sector([{'stock_code': '600132', 'sector': 'consumer', 'category': '啤酒', 'margin_base': 0.1}])
    assert grp['rows'][0]['sector_label'] == '啤酒'
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_group_by_sector_carves_out_whitelisted_category -v`
Expected: FAIL，`assert '啤酒' in groups`（当前按 sector 分组，啤酒落入 other/consumer）

- [ ] **Step 3: 写最小实现**

在 `app/routes/valuations.py` 的 `SECTOR_LABELS` 字典定义之后新增常量：

```python
CARVE_OUT_CATEGORIES = {'啤酒'}
```

然后把 `group_by_sector` 整体替换为：

```python
def group_by_sector(rows: list[dict]) -> list[dict]:
    """分组：category 命中 CARVE_OUT_CATEGORIES 则用分类名作独立顶级组，否则按 sector。
    组按标的数降序（并列按 key 稳定），组内按 Base 安全边际降序（None 末位）。
    sector 缺失归入「未分类」组；未知 sector 回退原始值。"""
    buckets: dict[str, list] = {}
    for r in rows:
        cat = r.get('category')
        key = cat if cat in CARVE_OUT_CATEGORIES else (r.get('sector') or '__none__')
        buckets.setdefault(key, []).append(r)
    groups = []
    for key, items in buckets.items():
        items = sorted(items, key=lambda x: (x.get('margin_base') is None, -(x.get('margin_base') or 0)))
        if key in CARVE_OUT_CATEGORIES:
            label = key
        elif key == '__none__':
            label = '未分类'
        else:
            label = SECTOR_LABELS.get(key, key)
        for r in items:
            r['sector_label'] = label
        groups.append({'sector': key, 'label': label, 'count': len(items), 'rows': items})
    groups.sort(key=lambda g: (-g['count'], g['sector']))
    return groups
```

- [ ] **Step 4: 运行测试确认通过（含全部既有 group_by_sector 测试不破）**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py -k group_by_sector -v`
Expected: PASS（新增 3 个 + 既有 8 个 group_by_sector 测试全绿）

- [ ] **Step 5: 提交**

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat(valuations): group_by_sector 支持白名单分类 carve-out"
```

---

## Task 4: index() 接入分类映射 + 端到端集成测试

**Files:**
- Modify: `app/routes/valuations.py:106-125`（`index`，仅 `enriched = ...` 一行前后）
- Test: `tests/test_valuations.py`

- [ ] **Step 1: 写失败测试**

追加（monkeypatch `load_category_map`，使集成测试不依赖 DB 数据）：

```python
def test_index_renders_beer_group_when_categorized(app_client, monkeypatch):
    from app.routes import valuations as val
    monkeypatch.setattr(val, 'load_category_map',
                        lambda: {'600132': '啤酒', '600600': '啤酒', '000729': '啤酒'})
    html = app_client.get('/valuations/').data.decode('utf-8')
    assert '啤酒' in html
    assert 'data-sector="啤酒"' in html
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_index_renders_beer_group_when_categorized -v`
Expected: FAIL，`assert '啤酒' in html`（index 尚未调用 load_category_map）

- [ ] **Step 3: 写最小实现**

在 `app/routes/valuations.py` 的 `index()` 里，把这一行：

```python
    enriched = _enrich(rows, prices)
```

替换为：

```python
    cat_map = load_category_map()
    enriched = _enrich(rows, prices, cat_map)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py::test_index_renders_beer_group_when_categorized -v`
Expected: PASS（1 passed）

- [ ] **Step 5: 全量回归 + 提交**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py -v`
Expected: PASS（全部测试绿）

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat(valuations): index 接入分类映射，啤酒独立成组"
```

---

## Task 5: 一次性 DB 写入（建「啤酒」分类 + 改挂 3 只票）

**Files:**
- Create（临时，跑完即删）: `scripts/_setup_beer_category.py`
- 写库: `data/stock.db`（`categories` + `stock_categories`）

> 非 seed：seed 铁律"已存在归属不覆盖"，而青岛/燕京要覆盖。故走一次性写入；之后由用户在分类管理 UI 维护。

- [ ] **Step 1: 写脚本**

Create `scripts/_setup_beer_category.py`：

```python
import sqlite3

conn = sqlite3.connect('data/stock.db')
c = conn.cursor()

c.execute("SELECT id FROM categories WHERE name='啤酒' AND parent_id IS NULL")
row = c.fetchone()
if row:
    beer_id = row[0]
else:
    c.execute("INSERT INTO categories (name, created_at) VALUES ('啤酒', datetime('now'))")
    beer_id = c.lastrowid

for code in ('600132', '600600', '000729'):
    c.execute("SELECT id FROM stock_categories WHERE stock_code=?", (code,))
    if c.fetchone():
        c.execute("UPDATE stock_categories SET category_id=? WHERE stock_code=?", (beer_id, code))
    else:
        c.execute("INSERT INTO stock_categories (stock_code, category_id) VALUES (?, ?)", (code, beer_id))

conn.commit()

print('beer_id =', beer_id)
for code in ('600132', '600600', '000729'):
    c.execute(
        "SELECT sc.stock_code, cat.name FROM stock_categories sc "
        "JOIN categories cat ON sc.category_id=cat.id WHERE sc.stock_code=?", (code,))
    print(c.fetchone())
conn.close()
```

- [ ] **Step 2: 运行脚本写库**

Run: `PYTHONIOENCODING=utf-8 python scripts/_setup_beer_category.py`
Expected 输出形如：
```
beer_id = 70
('600132', '啤酒')
('600600', '啤酒')
('000729', '啤酒')
```

- [ ] **Step 3: 删除一次性脚本（不入库）**

Run: `rm -f scripts/_setup_beer_category.py`

- [ ] **Step 4: 确认无残留待提交脚本**

Run: `rtk git status`
Expected: 无 `scripts/_setup_beer_category.py`（工作区干净，不需要提交）

---

## Task 6: 真实页面验证

**Files:** 无（仅验证）

- [ ] **Step 1: 启动应用**

Run（后台）: `python run.py`

- [ ] **Step 2: 打开估值页核对**

浏览器访问 `http://127.0.0.1:5000/valuations/`，确认：
- 出现独立「啤酒」分组，含 青岛啤酒 / 燕京啤酒 / 重庆啤酒 3 只
- 这 3 只不再出现在「消费」「其他」组
- 「啤酒」行板块列 badge 显示「啤酒」
- 点击「啤酒」组头可折叠；Base 列排序、A股/全部市场筛选对该组正常

- [ ] **Step 3: 全量单测最后一遍**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_valuations.py -v`
Expected: PASS（全绿）

---

## Self-Review

**Spec coverage：**
- §3 数据准备 → Task 5 ✅
- §4.1 CARVE_OUT_CATEGORIES → Task 3 ✅
- §4.2 load_category_map（异常守卫）→ Task 1 ✅
- §4.3 _enrich 附 category → Task 2 ✅
- §4.4 group_by_sector carve-out + sector_label → Task 3 ✅
- §4 index() 串联 → Task 4 ✅
- §5 模板零改动 → 计划未含模板任务，符合 ✅
- §6 测试（carve-out / 非白名单 / sector_label / _enrich / load_category_map 降级 / 既有全绿）→ Task 1-4 ✅
- §7 验收 → Task 6 ✅

**Placeholder scan：** 无 TBD/TODO/"类似上文"；每个代码步骤均含完整代码。✅

**Type consistency：** `load_category_map()→dict[str,str]`、`_enrich(rows, prices, cat_map=None)`、`group_by_sector(rows)`、常量 `CARVE_OUT_CATEGORIES`、行字段 `category` / `sector_label` 全程命名一致。✅
