# 盯盘页相对走势图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 盯盘页价值洼地区块新增一张多股归一叠加折线图，与价值洼地表共用 7/30/90 toggle；A+H 双重上市标的同时画 A、H 两条独立归一线。

**Architecture:** 服务端按周期归一（方案 A）——新端点 `/value-dip/api/relative?period=` 返回该周期已归一的序列；前端切 toggle 时重取重绘。核心归一逻辑抽成纯函数 `_build_series` 便于单测。A+H 对应关系走 `WATCH_CODES` 的可选 `ah` 字段。

**Tech Stack:** Flask blueprint (`value_dip_bp`)、`ValueDipService`、`UnifiedStockDataService.get_trend_data`、ECharts（base.html 全局已引入）、原生 JS（`value_dip.js` IIFE）。

## Global Constraints

- 响应中文；不写多余注释；不写 backup 文件（git 留痕）。
- 所有 git 命令前加 `rtk`（链式 `&&` 中也要）。`git add` 与 `git commit` 放同一条 Bash 命令链（防并行 session 抢 index）。
- 单测命令：`PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_value_dip_relative.py -v`（env 赋值必须在 `rtk` 之前）。
- 测试平铺 `tests/test_*.py`，不建子目录。
- 本功能改 `app/` 代码：执行阶段先经 `superpowers:using-git-worktrees` 建独立 worktree 隔离，勿直接动 main。
- `get_trend_data(codes, days=90)` 返回 `{'stocks':[{'stock_code','stock_name','data':[{date,open,high,low,close,volume,change_pct}]}], 'date_range':{...}}` —— list of dict，取 `{s['stock_code']: s.get('data',[])}`。
- `value_dip_bp` 的 `url_prefix='/value-dip'` 在 blueprint 自身；标准注册即带前缀。

---

### Task 1: WATCH_CODES 加 `ah` 字段（A+H 对应腿）

**Files:**
- Modify: `app/config/stock_codes.py:74-87`（`WATCH_CODES`）

**Interfaces:**
- Produces: `WATCH_CODES` 条目可选 `ah` 子字段 `{'code': str, 'market': str, 'name': str}`，仅真·A+H 双重上市标的携带。下游 `_build_series` / `get_relative_series` 消费。

**A+H 名单（逐一核实结论）：** 盯盘池 12 只中仅 2 只真·A+H——
- 天岳先进 H `2631.HK` ↔ A `688234`
- 江西铜业 H `0358.HK` ↔ A `600362`

其余非 A+H，不加 `ah`：北京君正/仕佳光子/三安光电/阳光电源（仅 A）、SK海力士（韩股）、英诺赛科/小米/腾讯/泡泡玛特（港股无 A）、沃尔核材 `9981.HK`（无确认 H↔A 对应，不硬猜）。

- [ ] **Step 1: 给天岳先进、江西铜业两条加 `ah` 字段**

把 `app/config/stock_codes.py` 中这两行：

```python
    {'code': '2631.HK',   'name': '天岳先进',  'market': 'HK'},
```
```python
    {'code': '0358.HK',   'name': '江西铜业',  'market': 'HK'},
```

分别改为：

```python
    {'code': '2631.HK',   'name': '天岳先进',  'market': 'HK',
     'ah': {'code': '688234', 'market': 'A', 'name': '天岳先进'}},
```
```python
    {'code': '0358.HK',   'name': '江西铜业',  'market': 'HK',
     'ah': {'code': '600362', 'market': 'A', 'name': '江西铜业'}},
```

- [ ] **Step 2: 语法自检**

Run: `PYTHONIOENCODING=utf-8 python -c "from app.config.stock_codes import WATCH_CODES; print([e['ah']['code'] for e in WATCH_CODES if e.get('ah')])"`
Expected: `['688234', '600362']`

- [ ] **Step 3: Commit**

```bash
rtk git add app/config/stock_codes.py && rtk git commit -m "feat(watch): WATCH_CODES 加 ah 字段（天岳/江西铜业 A+H 对应腿）"
```

---

### Task 2: `ValueDipService.get_relative_series` + 纯函数 `_build_series`

**Files:**
- Modify: `app/services/value_dip.py`（顶部加 import；类内加 `PERIOD_DAYS`/`_AH_SUFFIX`/`_build_series`/`get_relative_series`）
- Test: `tests/test_value_dip_relative.py`（新建）

**Interfaces:**
- Consumes: `WATCH_CODES`（含 Task 1 的 `ah`）、`unified_stock_data_service.get_trend_data`。
- Produces:
  - `ValueDipService._build_series(entries: list, trend_map: dict, period: str) -> list` —— 纯函数。`entries`=WATCH_CODES 形态；`trend_map`={code: [ohlc dict]}；返回 `[{code, name, market, label, dates:[str], values:[float]}]`，`values` 已归一为累计%（窗口起点=0.0），`dates`/`values` 等长。带 `ah` 的条目展开为主腿+ah腿两条，label 加 `(A)`/`(H)` 后缀；无 `ah` 的单股 label=name。
  - `ValueDipService.get_relative_series(period: str = '30d') -> list` —— 从 `WATCH_CODES` 收集 codes(+ah腿) 一次 `get_trend_data(days=90)`，构 trend_map，调 `_build_series`。
  - 模块级 `PERIOD_DAYS = {'7d':7,'30d':30,'90d':90}`。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_value_dip_relative.py`：

```python
from app.services.value_dip import ValueDipService, PERIOD_DAYS


def _mk(n, start=100.0, step=1.0):
    return [{'date': f'2026-01-{i + 1:02d}', 'close': start + i * step} for i in range(n)]


def test_normalize_base_zero():
    entries = [{'code': 'X', 'name': '甲', 'market': 'A'}]
    series = ValueDipService._build_series(entries, {'X': _mk(30)}, '30d')
    assert len(series) == 1
    s = series[0]
    assert s['values'][0] == 0.0
    assert s['label'] == '甲'
    assert len(s['dates']) == len(s['values'])


def test_normalize_pct():
    entries = [{'code': 'X', 'name': '甲', 'market': 'A'}]
    trend = {'X': [{'date': 'd1', 'close': 100.0}, {'date': 'd2', 'close': 110.0}]}
    series = ValueDipService._build_series(entries, trend, '30d')
    assert series[0]['values'] == [0.0, 10.0]


def test_window_by_trading_days():
    entries = [{'code': 'X', 'name': '甲', 'market': 'A'}]
    series = ValueDipService._build_series(entries, {'X': _mk(90)}, '7d')
    assert len(series[0]['values']) == 7
    assert PERIOD_DAYS['7d'] == 7


def test_ah_expansion_labels():
    entries = [{'code': '2631.HK', 'name': '天岳先进', 'market': 'HK',
                'ah': {'code': '688234', 'name': '天岳先进', 'market': 'A'}}]
    trend = {'2631.HK': _mk(30), '688234': _mk(30)}
    series = ValueDipService._build_series(entries, trend, '30d')
    assert sorted(s['label'] for s in series) == ['天岳先进(A)', '天岳先进(H)']


def test_degrade_missing_series():
    entries = [{'code': 'X', 'name': '甲', 'market': 'A'},
               {'code': 'Y', 'name': '乙', 'market': 'A'}]
    series = ValueDipService._build_series(entries, {'X': _mk(30)}, '30d')
    assert [s['code'] for s in series] == ['X']


def test_degrade_ah_leg_only():
    entries = [{'code': '2631.HK', 'name': '天岳先进', 'market': 'HK',
                'ah': {'code': '688234', 'name': '天岳先进', 'market': 'A'}}]
    series = ValueDipService._build_series(entries, {'2631.HK': _mk(30)}, '30d')
    assert [s['label'] for s in series] == ['天岳先进(H)']
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_value_dip_relative.py -v`
Expected: FAIL（`ImportError: cannot import name 'PERIOD_DAYS'` / `AttributeError: _build_series`）

- [ ] **Step 3: 实现服务方法**

在 `app/services/value_dip.py` 顶部 import 区加：

```python
from app.config.stock_codes import WATCH_CODES
```

在 `ValueDipService` 类外（模块级，import 之后）加：

```python
PERIOD_DAYS = {'7d': 7, '30d': 30, '90d': 90}
_AH_SUFFIX = {'A': 'A', 'HK': 'H'}
```

在 `ValueDipService` 类内加两个静态方法：

```python
    @staticmethod
    def _build_series(entries: list, trend_map: dict, period: str) -> list:
        days = PERIOD_DAYS.get(period, 30)
        series = []
        for e in entries:
            legs = [(e['code'], e['name'], e['market'], bool(e.get('ah')))]
            if e.get('ah'):
                ah = e['ah']
                legs.append((ah['code'], ah['name'], ah['market'], True))
            for code, name, market, paired in legs:
                data = trend_map.get(code) or []
                window = data[-days:] if len(data) > days else data
                pts = [(d.get('date'), d.get('close')) for d in window
                       if d.get('close') is not None]
                if len(pts) < 2:
                    continue
                base = float(pts[0][1])
                if base <= 0:
                    continue
                label = f"{name}({_AH_SUFFIX.get(market, market)})" if paired else name
                series.append({
                    'code': code, 'name': name, 'market': market, 'label': label,
                    'dates': [p[0] for p in pts],
                    'values': [round((float(p[1]) / base - 1) * 100, 2) for p in pts],
                })
        return series

    @staticmethod
    def get_relative_series(period: str = '30d') -> list:
        codes = []
        for e in WATCH_CODES:
            codes.append(e['code'])
            if e.get('ah'):
                codes.append(e['ah']['code'])
        trend_result = unified_stock_data_service.get_trend_data(codes, days=90)
        trend_map = {}
        if trend_result and trend_result.get('stocks'):
            for s in trend_result['stocks']:
                trend_map[s['stock_code']] = s.get('data', [])
        return ValueDipService._build_series(WATCH_CODES, trend_map, period)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_value_dip_relative.py -v`
Expected: PASS（6 passed）

- [ ] **Step 5: Commit**

```bash
rtk git add app/services/value_dip.py tests/test_value_dip_relative.py && rtk git commit -m "feat(watch): ValueDipService 相对走势归一序列 + 单测"
```

---

### Task 3: `/value-dip/api/relative` 路由

**Files:**
- Modify: `app/routes/value_dip.py`（加 `relative` 路由）
- Test: `tests/test_value_dip_relative.py`（追加端点测试）

**Interfaces:**
- Consumes: `ValueDipService.get_relative_series(period)`。
- Produces: `GET /value-dip/api/relative?period=7d|30d|90d` → `{'series': [...]}`（默认 30d，非法 period 归 30d）；异常 `{'error': str}, 500`。

- [ ] **Step 1: 追加失败测试**

在 `tests/test_value_dip_relative.py` 末尾追加：

```python
import pytest
from flask import Flask
from app.routes import value_dip_bp


@pytest.fixture
def client(monkeypatch):
    app = Flask(__name__)
    app.register_blueprint(value_dip_bp)
    monkeypatch.setattr(
        ValueDipService, 'get_relative_series',
        staticmethod(lambda period='30d': [
            {'code': 'X', 'name': '甲', 'market': 'A', 'label': '甲',
             'dates': ['d1', 'd2'], 'values': [0.0, 5.0]}]))
    return app.test_client()


def test_relative_endpoint_ok(client):
    resp = client.get('/value-dip/api/relative?period=7d')
    assert resp.status_code == 200
    data = resp.get_json()
    assert 'series' in data
    row = data['series'][0]
    assert len(row['dates']) == len(row['values'])
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_value_dip_relative.py::test_relative_endpoint_ok -v`
Expected: FAIL（404，路由未定义）

- [ ] **Step 3: 实现路由**

在 `app/routes/value_dip.py` 末尾加：

```python
@value_dip_bp.route('/api/relative')
def relative():
    try:
        period = request.args.get('period', '30d')
        if period not in ('7d', '30d', '90d'):
            period = '30d'
        data = ValueDipService.get_relative_series(period)
        return jsonify({'series': data})
    except Exception as e:
        logger.error(f'[价值洼地] 相对走势API错误: {e}')
        return jsonify({'error': str(e)}), 500
```

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_value_dip_relative.py -v`
Expected: PASS（7 passed）

- [ ] **Step 5: Commit**

```bash
rtk git add app/routes/value_dip.py tests/test_value_dip_relative.py && rtk git commit -m "feat(watch): /value-dip/api/relative 相对走势端点"
```

---

### Task 4: 前端图表（watch.html 容器 + value_dip.js 初始化/重绘）

**Files:**
- Modify: `app/templates/watch.html`（价值洼地区块内、toggle 下方、表格上方加 ECharts 容器）
- Modify: `app/static/js/value_dip.js`（加图初始化 + toggle 重取重绘）

**Interfaces:**
- Consumes: `GET /value-dip/api/relative?period=`（Task 3）、全局 `echarts`（base.html 已引入）、现有 `#period-toggle` / `currentPeriod`。

- [ ] **Step 1: 加图表容器**

在 `app/templates/watch.html` 价值洼地区块，把 toggle 行的结束 `</div>`（第 134 行 `</div>` 后）与表格 `<div class="card">`（第 135 行）之间插入：

```html
    <div class="card mb-3">
        <div class="card-body">
            <div id="relative-chart" style="height: 380px;"></div>
        </div>
    </div>
```

即结构变为：toggle 行 `</div>` → 相对走势图 card → 价值洼地表 card。

- [ ] **Step 2: value_dip.js 加图逻辑**

在 `app/static/js/value_dip.js` 的 IIFE 内，`let currentPeriod = '30d';` 下一行加：

```javascript
    let relChart = null;
```

`DOMContentLoaded` 回调里 `loadStocks();` 下一行加：

```javascript
        loadRelative();
```

toggle handler 内、`render();` 之后加：

```javascript
            loadRelative();
```

在 `render()` 函数之后（IIFE 内）加三个函数：

```javascript
    function ensureChart() {
        if (!relChart) {
            relChart = echarts.init(document.getElementById('relative-chart'));
            window.addEventListener('resize', () => relChart && relChart.resize());
        }
        return relChart;
    }

    async function loadRelative() {
        try {
            const resp = await fetch('/value-dip/api/relative?period=' + currentPeriod);
            const data = await resp.json();
            renderRelative(data.series || []);
        } catch (e) {
            console.error('加载相对走势失败:', e);
            const el = document.getElementById('relative-chart');
            if (el) el.innerHTML = '<div class="text-center text-muted py-5">加载失败</div>';
        }
    }

    function renderRelative(series) {
        const chart = ensureChart();
        const dateSet = new Set();
        series.forEach(s => s.dates.forEach(d => dateSet.add(d)));
        const xDates = [...dateSet].sort();
        const seriesOpt = series.map(s => {
            const m = {};
            s.dates.forEach((d, i) => { m[d] = s.values[i]; });
            return {
                name: s.label,
                type: 'line',
                showSymbol: false,
                connectNulls: true,
                data: xDates.map(d => (d in m ? m[d] : null)),
            };
        });
        chart.setOption({
            tooltip: { trigger: 'axis', order: 'valueDesc',
                       valueFormatter: v => (v == null ? '-' : v + '%') },
            legend: { type: 'scroll', top: 0 },
            grid: { left: 48, right: 16, top: 40, bottom: 30 },
            xAxis: { type: 'category', data: xDates },
            yAxis: { type: 'value', axisLabel: { formatter: '{value}%' } },
            series: seriesOpt,
        }, true);
    }
```

- [ ] **Step 3: 手动验证（前端无自动测试）**

Run: `SCHEDULER_ENABLED=0 python run.py`（后台启动），浏览器开 `http://127.0.0.1:5000/watch`。
Expected:
- 价值洼地区块 toggle 下方出现折线图，约 14 条线（12 盯盘股 + 天岳/江西铜业各多一条 A 腿），起点归零。
- 天岳先进(A)/(H)、江西铜业(A)/(H) 图例可见。
- 切 7天/30天/90天：图与表同步刷新，窗口/归一随之变化。
- legend 点击可隐藏/显示单线；tooltip 悬停显示各线累计%并按值排序。
- 关闭 run.py。

- [ ] **Step 4: Commit**

```bash
rtk git add app/templates/watch.html app/static/js/value_dip.js && rtk git commit -m "feat(watch): 盯盘页相对走势归一叠加折线图（共用 7/30/90 toggle）"
```

---

## 自审记录（写计划后回查 spec）

- **Spec 覆盖**：图表语义(归一叠加)→Task2/4；数据流方案A(服务端归一)→Task2/3；A+H via ah 字段→Task1/2；范围=盯盘池→Task2；ECharts 位置/共用 toggle/legend/tooltip/connectNulls→Task4；错误处理(端点500/单股跳过/ah腿跳过/前端降级)→Task2(跳过)/Task3(500)/Task4(前端)；测试(归一/截窗/A+H展开/降级/端点契约)→Task2/3。无遗漏。
- **占位符**：无 TBD/TODO；每步含完整代码/命令/预期。
- **类型一致**：`_build_series`/`get_relative_series`/`PERIOD_DAYS` 命名在 Task2 定义、Task3 消费一致；序列字段 `{code,name,market,label,dates,values}` 前后端一致；端点 `{'series':[...]}` 契约 Task3 产出、Task4 消费一致。
