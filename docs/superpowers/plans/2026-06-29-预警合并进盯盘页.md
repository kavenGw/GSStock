# 预警合并进盯盘页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 删除独立 `/alert` 预警页，把技术信号能力收缩进盯盘页 summary 表新增「信号」列，仅覆盖 `WATCH_CODES`，并保留可调阈值设置。

**Architecture:** 新增轻量后端端点 `GET /watch/signals` 批量返回盯盘股 60 日 OHLC；前端 `watch.js` 复用未改动的 `signal-detector.js` 逐股计算信号，渲染进 summary 表新列；阈值存独立 localStorage key，改动后纯前端重算重渲染。最后删除 `/alert` 页及其专属资源。

**Tech Stack:** Flask（蓝图路由）、pytest（路由测试 + monkeypatch）、原生 JS（ECharts 页面，无前端测试框架）、Bootstrap modal。

## Global Constraints

- 响应中文；不写多余注释；不写 backup 文件（git 留痕足够）。
- 所有 git/pytest 命令前加 `rtk`，链式 `&&` 中也要。
- 单测：`PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/ -v`；env 赋值必须在 `rtk` 之前。
- 路由测试用 `Flask() + register_blueprint(watch_bp)` 直接注入，避免 `create_app()` 副作用（启 17 任务 + crawl4ai + LLM）。
- 单测放 `tests/test_*.py` 平铺，不用子目录。
- `git add` 与 `git commit` 放进同一条 Bash 命令链，用精确路径，勿 `git add -A`（并行 session 抢 index）。
- `signal-detector.js` / `SignalCacheService` / `price_alert` 策略 / stock_detail 的 wyckoff backtest **不得删除**（仍被其他页面/策略使用）。

---

### Task 1: 后端端点 `GET /watch/signals`

**Files:**
- Modify: `app/routes/watch.py`（在文件末尾 `earnings()` 路由后追加新路由）
- Test: `tests/test_watch_signals.py`（新建）

**Interfaces:**
- Consumes: `WatchService.get_watch_codes()`（返回 `WATCH_CODES` 的 code 列表）；`unified_stock_data_service.get_trend_data(codes, days=60)`（返回 `{'stocks': [{'stock_code','stock_name','data':[OHLC...]}, ...]}`）。
- Produces: `GET /watch/signals` → `{'success': True, 'stocks': [{'stock_code': str, 'data': list}, ...]}`。前端 Task 2 消费此结构。

- [ ] **Step 1: 写失败测试**

新建 `tests/test_watch_signals.py`：

```python
from flask import Flask
from app.routes import watch_bp


def _watch_client():
    app = Flask(__name__)
    app.register_blueprint(watch_bp)
    return app.test_client()


def test_watch_signals_returns_ohlc_per_code(monkeypatch):
    import app.services.unified_stock_data as usd

    def fake_get_trend_data(codes, days=60):
        assert days == 60
        return {'stocks': [
            {'stock_code': c, 'stock_name': c,
             'data': [{'date': '2026-06-01', 'open': 1, 'high': 2,
                       'low': 1, 'close': 1.5, 'volume': 100}]}
            for c in codes
        ]}

    monkeypatch.setattr(usd.unified_stock_data_service,
                        'get_trend_data', fake_get_trend_data)

    resp = _watch_client().get('/watch/signals')
    assert resp.status_code == 200
    data = resp.get_json()
    assert data['success'] is True
    codes = {s['stock_code'] for s in data['stocks']}
    assert '2631.HK' in codes and '300223' in codes
    assert all('data' in s and isinstance(s['data'], list) for s in data['stocks'])
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_watch_signals.py -v`
Expected: FAIL（404 Not Found，端点未定义）

- [ ] **Step 3: 实现端点**

在 `app/routes/watch.py` 末尾（`earnings()` 路由之后）追加：

```python
@watch_bp.route('/signals')
def signals():
    """批量返回盯盘股 60 日 OHLC，供前端计算技术信号"""
    from app.services.unified_stock_data import unified_stock_data_service

    codes = WatchService.get_watch_codes()
    if not codes:
        return jsonify({'success': True, 'stocks': []})

    trend = unified_stock_data_service.get_trend_data(codes, days=60)
    stocks = trend.get('stocks', []) if trend else []
    return jsonify({'success': True, 'stocks': [
        {'stock_code': s.get('stock_code'), 'data': s.get('data', [])}
        for s in stocks
    ]})
```

- [ ] **Step 4: 运行测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_watch_signals.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
rtk git add app/routes/watch.py tests/test_watch_signals.py && rtk git commit -m "feat(watch): 新增 /watch/signals 批量返回盯盘股60日OHLC"
```

---

### Task 2: 前端 summary 表新增「信号」列

**Files:**
- Modify: `app/templates/watch.html`（`extra_js` 块加 signal-detector.js 引用）
- Modify: `app/static/js/watch.js`（Watch 对象加状态/方法，改 `_renderSummaryTable`，loadList 中调用 `loadSignals`）
- Test: 手动验证（项目无前端测试框架）

**Interfaces:**
- Consumes: Task 1 的 `GET /watch/signals`；全局 `SignalDetector.detectAll(ohlcData, thresholds)` → `{buySignals, sellSignals, alerts}`，每个信号含 `{type:'buy'|'sell'|'neutral', name, description}`，pattern 信号另含 `index`。
- Produces: `Watch.signals`（`{code: [signal...]}`）、`Watch.ohlc`、`Watch.computeSignals()`、`Watch._signalThresholds()`、`Watch._renderSignalBadges(code)`、`Watch.loadSignals()`。Task 3 复用 `_signalThresholds`/`computeSignals`/`_updateAllSummaryTables`。

- [ ] **Step 1: watch.html 引入 signal-detector.js**

修改 `app/templates/watch.html` 的 `extra_js` 块（当前为单条 watch.js），改为：

```html
{% block extra_js %}
<script src="{{ url_for('static', filename='js/signal-detector.js') }}"></script>
<script src="{{ url_for('static', filename='js/watch.js') }}"></script>
{% endblock %}
```

- [ ] **Step 2: watch.js 加状态字段与阈值/信号方法**

在 `app/static/js/watch.js` 的 `Watch` 对象状态字段区（`tdSequentialIntraday: {},` 之后）追加：

```javascript
    ohlc: {},
    signals: {},
    SIGNAL_THRESHOLDS_KEY: 'watchSignalThresholds',
    DEFAULT_THRESHOLDS: { rsiOverbought: 70, rsiOversold: 30, volumeRatio: 2.0, volumeShrinkRatio: 0.5, bollingerWidth: 2 },
```

在 `_getMarketGroups()` 方法**之前**（`// --- 市场分组工具 ---` 注释上方）插入以下方法块：

```javascript
    // --- 技术信号 ---
    _signalThresholds() {
        try {
            const raw = localStorage.getItem(this.SIGNAL_THRESHOLDS_KEY);
            if (!raw) return { ...this.DEFAULT_THRESHOLDS };
            return { ...this.DEFAULT_THRESHOLDS, ...JSON.parse(raw) };
        } catch {
            return { ...this.DEFAULT_THRESHOLDS };
        }
    },

    _saveSignalThresholds(t) {
        try {
            localStorage.setItem(this.SIGNAL_THRESHOLDS_KEY, JSON.stringify(t));
        } catch (e) {
            console.warn('[Watch] 阈值保存失败:', e);
        }
    },

    async loadSignals() {
        try {
            const resp = await fetch('/watch/signals');
            const result = await resp.json();
            if (!result.success) return;
            this.ohlc = {};
            (result.stocks || []).forEach(s => { this.ohlc[s.stock_code] = s.data || []; });
            this.computeSignals();
            this._updateAllSummaryTables();
        } catch (e) {
            console.error('[Watch] signals load failed:', e);
        }
    },

    computeSignals() {
        this.signals = {};
        if (typeof SignalDetector === 'undefined') return;
        const thresholds = this._signalThresholds();
        for (const stock of this.stocks) {
            const code = stock.stock_code;
            const ohlc = this.ohlc[code];
            if (!ohlc || ohlc.length < 20) continue;
            try {
                const result = SignalDetector.detectAll(ohlc, thresholds);
                const recentIdx = ohlc.length - 3;
                const patterns = [...result.buySignals, ...result.sellSignals]
                    .filter(s => typeof s.index === 'number' && s.index >= recentIdx);
                const merged = [...result.alerts, ...patterns];
                if (merged.length > 0) this.signals[code] = merged;
            } catch (e) {
                console.warn(`[Watch] 信号计算失败 ${code}:`, e);
            }
        }
    },

    _renderSignalBadges(code) {
        const signals = this.signals[code] || [];
        if (signals.length === 0) return '<span class="text-muted">—</span>';
        const cls = { buy: 'signal-buy', sell: 'signal-sell', neutral: 'signal-watch' };
        return signals.map(s => {
            const c = cls[s.type] || 'signal-watch';
            const title = (s.description || '').replace(/"/g, '&quot;');
            return `<span class="entry-signal ${c} me-1" title="${title}">${s.name}</span>`;
        }).join('');
    },
```

- [ ] **Step 3: 改 `_renderSummaryTable` 加「信号」列**

在 `app/static/js/watch.js` 的 `_renderSummaryTable` 中，修改表头（当前 `<th>股票</th><th class="text-end">涨跌%</th><th style="min-width:220px">支撑 / 现价 / 压力</th><th>AI摘要</th>`），在「涨跌%」与「支撑/现价/压力」之间插入「信号」列：

```javascript
        let html = `<table class="table table-sm table-hover stock-summary-table mb-0">
            <thead><tr>
                <th>股票</th><th class="text-end">涨跌%</th>
                <th>信号</th>
                <th style="min-width:220px">支撑 / 现价 / 压力</th>
                <th>AI摘要</th>
            </tr></thead><tbody>`;
```

在循环体末尾的行模板（当前 `<td class="text-end ${pctClass} fw-bold">${pctDisplay}</td>` 之后），插入信号单元格：

```javascript
            html += `<tr>
                <td class="stock-name">${name}${tdBadge}</td>
                <td class="text-end ${pctClass} fw-bold">${pctDisplay}</td>
                <td>${this._renderSignalBadges(code)}</td>
                <td>${rangeBarHtml}</td>
                <td>${aiHtml}</td>
            </tr>`;
```

- [ ] **Step 4: loadList 中触发信号加载**

在 `app/static/js/watch.js` 的 `loadList()` 中，`await this.loadAllCharts();` 之后、`this._saveToStore();` 之前插入：

```javascript
            this.loadSignals();
```

（fire-and-forget：summary 表先以 `—` 渲染，OHLC 到位后 `_updateAllSummaryTables` 补填，与 chart 异步填充模式一致。）

- [ ] **Step 5: 手动验证**

启动应用：`rtk python run.py`，浏览器开 http://127.0.0.1:5000/watch/

Expected:
- 每市场 summary 表出现「信号」列，位于「涨跌%」右、「支撑/现价/压力」左。
- 有信号的股票显示彩色徽标（买=绿/卖=红/中性=灰），鼠标悬停 title 显示描述；无信号显示 `—`。
- 浏览器 Console 有 `[SignalDetector] 检测完成` 日志、无报错。

- [ ] **Step 6: 提交**

```bash
rtk git add app/templates/watch.html app/static/js/watch.js && rtk git commit -m "feat(watch): summary表新增技术信号列（复用signal-detector）"
```

---

### Task 3: 盯盘页阈值设置弹窗

**Files:**
- Modify: `app/templates/watch.html`（page-header 加齿轮按钮 + 设置 modal）
- Modify: `app/static/js/watch.js`（`showSignalSettings()` / `saveSignalSettings()`）
- Test: 手动验证

**Interfaces:**
- Consumes: Task 2 的 `_signalThresholds()` / `_saveSignalThresholds(t)` / `computeSignals()` / `_updateAllSummaryTables()`。
- Produces: `Watch.showSignalSettings()`、`Watch.saveSignalSettings()`，绑定到 modal 按钮。

- [ ] **Step 1: watch.html 加齿轮按钮**

修改 `app/templates/watch.html` 的 page-header（当前 `<div class="d-flex justify-content-between align-items-center">` 内只有左侧标题块），在标题块 `</div>` 之后、外层 `</div>` 之前加右侧按钮：

```html
        <div>
            <button class="btn btn-outline-secondary btn-sm" onclick="Watch.showSignalSettings()">
                <i class="bi bi-gear"></i> 信号设置
            </button>
        </div>
```

- [ ] **Step 2: watch.html 加设置 modal**

在 `app/templates/watch.html` 的 `<div id="stockCards" class="d-none"></div>` 之后、`{% endblock %}` 之前插入：

```html
<!-- 信号阈值设置弹窗 -->
<div class="modal fade" id="signalSettingsModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"><i class="bi bi-gear"></i> 信号阈值设置</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="mb-3">
                    <label class="form-label">RSI 超买阈值：<span id="wsRsiOverboughtValue">70</span></label>
                    <input type="range" class="form-range" min="60" max="90" value="70" id="wsRsiOverboughtRange"
                           oninput="document.getElementById('wsRsiOverboughtValue').textContent = this.value">
                </div>
                <div class="mb-3">
                    <label class="form-label">RSI 超卖阈值：<span id="wsRsiOversoldValue">30</span></label>
                    <input type="range" class="form-range" min="10" max="40" value="30" id="wsRsiOversoldRange"
                           oninput="document.getElementById('wsRsiOversoldValue').textContent = this.value">
                </div>
                <div class="mb-3">
                    <label class="form-label">放量倍数：<span id="wsVolumeRatioValue">2.0</span></label>
                    <input type="range" class="form-range" min="1.5" max="5" step="0.5" value="2" id="wsVolumeRatioRange"
                           oninput="document.getElementById('wsVolumeRatioValue').textContent = parseFloat(this.value).toFixed(1)">
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
                <button type="button" class="btn btn-primary" onclick="Watch.saveSignalSettings()">保存</button>
            </div>
        </div>
    </div>
</div>
```

- [ ] **Step 3: watch.js 加 showSignalSettings / saveSignalSettings**

在 `app/static/js/watch.js` 的 `_renderSignalBadges(code)` 方法之后追加：

```javascript
    showSignalSettings() {
        const t = this._signalThresholds();
        const set = (id, val) => { const el = document.getElementById(id); if (el) el.value = val; };
        const txt = (id, val) => { const el = document.getElementById(id); if (el) el.textContent = val; };
        set('wsRsiOverboughtRange', t.rsiOverbought); txt('wsRsiOverboughtValue', t.rsiOverbought);
        set('wsRsiOversoldRange', t.rsiOversold); txt('wsRsiOversoldValue', t.rsiOversold);
        set('wsVolumeRatioRange', t.volumeRatio); txt('wsVolumeRatioValue', parseFloat(t.volumeRatio).toFixed(1));
        const modal = new bootstrap.Modal(document.getElementById('signalSettingsModal'));
        modal.show();
    },

    saveSignalSettings() {
        const t = this._signalThresholds();
        t.rsiOverbought = parseInt(document.getElementById('wsRsiOverboughtRange').value, 10);
        t.rsiOversold = parseInt(document.getElementById('wsRsiOversoldRange').value, 10);
        t.volumeRatio = parseFloat(document.getElementById('wsVolumeRatioRange').value);
        this._saveSignalThresholds(t);
        this.computeSignals();
        this._updateAllSummaryTables();
        bootstrap.Modal.getInstance(document.getElementById('signalSettingsModal')).hide();
    },
```

- [ ] **Step 4: 手动验证**

启动应用，开 /watch/，点「信号设置」：

Expected:
- 弹窗显示 3 个滑块，值与当前阈值一致；拖动时数值实时更新。
- 调整 RSI 超买/超卖、放量倍数后点保存 → 弹窗关闭，summary 表信号列即时重算（如调低超买阈值后更多股出现 RSI超买徽标）。
- 刷新页面后再开设置，阈值保持（localStorage `watchSignalThresholds`）；隔日访问（WatchStore 每日清缓存）阈值仍在（key 不带 `watch_` 前缀，不被清）。

- [ ] **Step 5: 提交**

```bash
rtk git add app/templates/watch.html app/static/js/watch.js && rtk git commit -m "feat(watch): 信号阈值设置弹窗（独立localStorage持久化）"
```

---

### Task 4: 删除独立 `/alert` 预警页

**Files:**
- Delete: `app/routes/alert.py`、`app/templates/alert.html`、`app/static/js/alert-page.js`、`app/static/css/alert.css`、`app/services/backtest.py`
- Modify: `app/routes/__init__.py`（删 `alert_bp` 定义，L13）、`app/__init__.py`（import 列表 L273 去 `alert_bp` + register L284）、`app/templates/base.html`（删导航「预警」入口 L25）
- Test: `tests/test_alert_removed.py`（新建）

**Interfaces:**
- Consumes: 无（纯删除 + 解线）。
- Produces: `app.routes` 不再暴露 `alert_bp`；`app.routes.alert` 模块不可导入。

- [ ] **Step 1: 写失败测试**

新建 `tests/test_alert_removed.py`：

```python
import importlib
import pytest


def test_alert_bp_not_exported():
    import app.routes as routes
    importlib.reload(routes)
    assert not hasattr(routes, 'alert_bp')


def test_alert_module_gone():
    with pytest.raises(ModuleNotFoundError):
        importlib.import_module('app.routes.alert')


def test_backtest_service_gone():
    with pytest.raises(ModuleNotFoundError):
        importlib.import_module('app.services.backtest')
```

- [ ] **Step 2: 运行测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_alert_removed.py -v`
Expected: FAIL（alert_bp 仍存在、模块仍可导入）

- [ ] **Step 3: 删除文件**

```bash
rtk git rm -q app/routes/alert.py app/templates/alert.html app/static/js/alert-page.js app/static/css/alert.css app/services/backtest.py
```

- [ ] **Step 4: 解线 `app/routes/__init__.py`**

删除 `app/routes/__init__.py` 第 13 行：

```python
alert_bp = Blueprint('alert', __name__, url_prefix='/alert')
```

- [ ] **Step 5: 解线 `app/__init__.py`**

修改 `app/__init__.py` L273 的 import 列表，去掉 `alert_bp,`（删除 `heavy_metals_bp, alert_bp, briefing_bp` 中的 `alert_bp, `，变为 `heavy_metals_bp, briefing_bp`）。

删除 `app/__init__.py` L284：

```python
    app.register_blueprint(alert_bp)
```

- [ ] **Step 6: 删导航入口 `app/templates/base.html`**

删除 `app/templates/base.html` 第 25 行：

```html
                <a class="nav-link" href="{{ url_for('alert.index') }}">预警</a>
```

- [ ] **Step 7: 运行测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_alert_removed.py -v`
Expected: PASS

- [ ] **Step 8: 全量回归 + 应用启动验证**

Run（输出重定向到会话 scratchpad 再 grep——避开 create_app 触发的 crawl4ai stdout 把 pytest 摘要顶出可见区）：
`PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/ -q > "$SCRATCHPAD/pytest_out.txt" 2>&1; grep -E "passed|failed|error" "$SCRATCHPAD/pytest_out.txt"`
（`$SCRATCHPAD` 替换为本会话 scratchpad 绝对路径）
Expected: 全绿，无因删除引发的 import 错误。

再启动应用 `rtk python run.py` 确认无 `BuildError`（base.html 不再引用 `alert.index`），导航无「预警」、仍有「盯盘」。

- [ ] **Step 9: 提交**

```bash
rtk git add app/routes/__init__.py app/__init__.py app/templates/base.html tests/test_alert_removed.py && rtk git commit -m "refactor(alert): 删除独立预警页，能力已并入盯盘页"
```

---

## 收尾验证（全部任务完成后）

- [ ] 全量单测通过：`PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/ -q`
- [ ] /watch/ 信号列正常、阈值设置可用且持久化
- [ ] /alert 不再可达，导航无「预警」
- [ ] `rtk git grep -n "alert_bp\|alert\.index\|alert-page\|BacktestService"` 仅命中 graphify-out（陈旧图谱，可忽略），无 app/ 代码残留
