# 高点回退提醒增加支撑位/压力位 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在每日简报「高点回退提醒」每只股末尾追加下方最近支撑 + 上方最近压力（带距现价百分比），保持单行紧凑格式。

**Architecture:** 复用现成 `app/utils/support_resistance.py:calculate_support_resistance`。在 `ValueDipService._calc_stock_changes` 里用已在手的 90 日 OHLC 算 S/R（零额外 API），经 `get_pullback_ranking` 透传，最后在 `_format_pullback_message` 渲染。

**Tech Stack:** Python / Flask / pytest。

## Global Constraints

- 响应中文；不写多余注释；不写 backup 文件
- 所有 git/pytest 命令前加 `rtk`
- 测试放 `tests/test_*.py` 平铺目录，不建子目录
- 位价保留 2 位小数；百分比用 `{:+.2f}%` 含正负号（支撑为负、压力为正）
- 数据不足 20 日 / 单侧缺失时，缺失部分整段省略，回退现有格式
- 单测运行：`PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest <file> -v`

---

### Task 1: `_calc_stock_changes` 计算 S/R 并经 `get_pullback_ranking` 透传

**Files:**
- Modify: `app/services/value_dip.py`（`_calc_stock_changes` 末尾加 S/R；`get_pullback_ranking` 结果 dict 加两字段）
- Test: `tests/test_pullback_support_resistance.py`（新建）

**Interfaces:**
- Consumes: `app.utils.support_resistance.calculate_support_resistance(highs, lows, closes) -> {'support': [float...], 'resistance': [float...]}`
- Produces:
  - `ValueDipService._calc_stock_changes(code, name, data)` 返回的 info dict 新增 `info['support']: float | None`、`info['resistance']: float | None`（下方最近支撑=support 列表中 < price 的最大值；上方最近压力=resistance 列表中 > price 的最小值；不足则 None）
  - `ValueDipService.get_pullback_ranking(days=90)` 返回的每个 dict 新增 `'support'` / `'resistance'` 键（透传，可能为 None）

- [ ] **Step 1: 写失败测试**

追加到新建文件 `tests/test_pullback_support_resistance.py`：

```python
from app.services.value_dip import ValueDipService


def _mk_ohlc(closes):
    """把收盘价序列造成 OHLC dict 列表（high=close+2, low=close-2）"""
    return [
        {'date': f'2026-06-{i + 1:02d}', 'open': c,
         'high': c + 2, 'low': c - 2, 'close': c, 'volume': 1000}
        for i, c in enumerate(closes)
    ]


# 25 日：先涨到 120 再回落到 100，制造下方支撑与上方压力
_CLOSES_25 = [95, 96, 98, 100, 103, 106, 110, 113, 116, 118, 120, 119, 117,
              115, 113, 111, 109, 107, 105, 103, 102, 101, 100, 100, 100]


def test_calc_changes_attaches_support_resistance():
    info = ValueDipService._calc_stock_changes('300223', '北京君正', _mk_ohlc(_CLOSES_25))
    assert info['support'] is not None
    assert info['resistance'] is not None
    assert info['support'] < info['price'] < info['resistance']


def test_calc_changes_sr_none_when_insufficient_data():
    info = ValueDipService._calc_stock_changes('300223', '北京君正', _mk_ohlc(_CLOSES_25[:10]))
    assert info['support'] is None
    assert info['resistance'] is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_pullback_support_resistance.py -v`
Expected: FAIL —— `KeyError: 'support'`（info dict 尚无该键）

- [ ] **Step 3: 在 `_calc_stock_changes` 末尾算 S/R**

`app/services/value_dip.py` 顶部 import 区加：

```python
from app.utils.support_resistance import calculate_support_resistance
```

在 `_calc_stock_changes` 的 `return info` 之前插入（`info['price']` 已在上文算好）：

```python
        info['support'] = None
        info['resistance'] = None
        if info['price']:
            triples = [(float(d['high']), float(d['low']), float(d['close']))
                       for d in data
                       if d.get('high') is not None
                       and d.get('low') is not None
                       and d.get('close') is not None]
            if len(triples) >= 20:
                sr = calculate_support_resistance(
                    [t[0] for t in triples],
                    [t[1] for t in triples],
                    [t[2] for t in triples])
                below = [s for s in sr['support'] if s < info['price']]
                above = [r for r in sr['resistance'] if r > info['price']]
                if below:
                    info['support'] = max(below)
                if above:
                    info['resistance'] = min(above)
```

- [ ] **Step 4: 在 `get_pullback_ranking` 透传字段**

`get_pullback_ranking` 里构造 `stocks.append({...})` 的 dict 追加两行：

```python
                    'high': high,
                    'pullback_pct': pullback,
                    'support': s.get('support'),
                    'resistance': s.get('resistance'),
```

- [ ] **Step 5: 跑测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_pullback_support_resistance.py -v`
Expected: PASS（2 passed）

- [ ] **Step 6: 提交**

```bash
rtk git add app/services/value_dip.py tests/test_pullback_support_resistance.py && rtk git commit -m "feat(value_dip): _calc_stock_changes 计算下方支撑/上方压力并透传"
```

---

### Task 2: `_format_pullback_message` 渲染 S/R 后缀

**Files:**
- Modify: `app/strategies/daily_briefing/__init__.py`（`_format_pullback_message`）
- Test: `tests/test_pullback_support_resistance.py`（追加）

**Interfaces:**
- Consumes: `get_pullback_ranking` 产出的 dict，含 `price` / `support` / `resistance`（后两者可能 None）
- Produces: 每行在 `→ *{pullback}%*` 后按需追加 ` | 下方支撑{价:.2f}({距现价%:+.2f}%)` 与 ` | 上方压力{价:.2f}({距现价%:+.2f}%)`

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_pullback_support_resistance.py`：

```python
from app.strategies.daily_briefing import DailyBriefingStrategy


def test_format_renders_support_and_resistance():
    stocks = [{'code': '300223', 'name': '北京君正', 'market': 'A',
               'price': 100.0, 'high': 120.0, 'pullback_pct': -16.67,
               'support': 98.2, 'resistance': 105.0}]
    msg = DailyBriefingStrategy._format_pullback_message(stocks)
    assert '下方支撑98.20(-1.80%)' in msg
    assert '上方压力105.00(+5.00%)' in msg


def test_format_omits_missing_sr():
    stocks = [{'code': '300223', 'name': '北京君正', 'market': 'A',
               'price': 100.0, 'high': 120.0, 'pullback_pct': -16.67,
               'support': None, 'resistance': None}]
    msg = DailyBriefingStrategy._format_pullback_message(stocks)
    assert '下方支撑' not in msg and '上方压力' not in msg


def test_format_renders_single_side():
    stocks = [{'code': '300223', 'name': '北京君正', 'market': 'A',
               'price': 100.0, 'high': 120.0, 'pullback_pct': -16.67,
               'support': 98.2, 'resistance': None}]
    msg = DailyBriefingStrategy._format_pullback_message(stocks)
    assert '下方支撑98.20(-1.80%)' in msg and '上方压力' not in msg
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_pullback_support_resistance.py -k format -v`
Expected: FAIL —— `test_format_renders_support_and_resistance` 断言不到 `下方支撑…`（现格式无后缀）

- [ ] **Step 3: 改写 `_format_pullback_message`**

`app/strategies/daily_briefing/__init__.py` 的 `_format_pullback_message` 整体替换为：

```python
    @staticmethod
    def _format_pullback_message(stocks: list) -> str:
        lines = ['📉 *高点回退提醒*（90日高点）\n']
        for s in stocks:
            line = (
                f"  · {s['name']}（{s['market']}）"
                f" 现价{s['price']} / 高点{s['high']}"
                f" → *{s['pullback_pct']}%*"
            )
            price = s.get('price')
            if price and s.get('support') is not None:
                sp = s['support']
                pct = (sp - price) / price * 100
                line += f" | 下方支撑{sp:.2f}({pct:+.2f}%)"
            if price and s.get('resistance') is not None:
                rp = s['resistance']
                pct = (rp - price) / price * 100
                line += f" | 上方压力{rp:.2f}({pct:+.2f}%)"
            lines.append(line)
        return '\n'.join(lines)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_pullback_support_resistance.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: 跑回归确认旧测试不破**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 rtk python -m pytest tests/test_value_dip_briefing.py -v`
Expected: PASS（`_format_pullback_message` 对无 support/resistance 键的旧输入用 `.get()` 兜底，向后兼容）

- [ ] **Step 6: 提交**

```bash
rtk git add app/strategies/daily_briefing/__init__.py tests/test_pullback_support_resistance.py && rtk git commit -m "feat(briefing): 高点回退提醒渲染下方支撑/上方压力位"
```

---

## Self-Review

- **Spec coverage**：数据来源（复用 util、零 API）→ Task 1 Step 3；取值逻辑（max support / min resistance）→ Task 1 Step 3；数据流透传 → Task 1 Step 4；渲染格式与百分比 → Task 2 Step 3；边界（不足 20 日 / 单侧 / 两侧无）→ Task 1 Step 3 + Task 2 tests；测试 5 项 → Task 1/2 覆盖。全覆盖。
- **Placeholder scan**：无 TBD/TODO，所有代码步含完整代码。
- **Type consistency**：`support`/`resistance` 在 Task 1 定义为 `float | None`，Task 2 用 `s.get('support')`/`s.get('resistance')` 消费，一致；`price` 键名两处一致。
