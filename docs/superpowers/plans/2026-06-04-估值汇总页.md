# 股票估值汇总页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增一个 Flask 页面 `/valuations`，一行一只股展示 Bear/Base/Bull 三档每股内在价值、实时当前价与安全边际，并提供「刷新」按钮按需拉取实时价格。

**Architecture:** 独立 `docs/stock-analytics/valuations.yaml` 为唯一数据源（与文档 frontmatter 解耦）。新建 `valuations_bp` blueprint：`GET /valuations/` 用缓存价渲染表格，`GET /valuations/api/prices?force=1` 用 `force_refresh=True` 批量拉价返回 JSON 供前端原地更新。存量三档估值由一次性 agent 抽取脚本填充 yaml（脚本不入库）。

**Tech Stack:** Flask + Jinja2 + Bootstrap5 + 原生 JS（fetch）；PyYAML；`UnifiedStockDataService`（实时价）；pytest。

---

## 已核实的代码事实（实现前必读）

- Blueprint 在 `app/routes/__init__.py` 第 1-22 行集中定义并 import，在 `app/__init__.py` 第 254-271 行集中 `register_blueprint`。
- 路由模式参考 `app/routes/supply_chain.py`（`render_template` + `/api/<x>` `jsonify`）。
- 实时价单例：`from app.services import unified_stock_data_service`（`app/services/__init__.py` 的模块级单例）。
- `get_realtime_prices(stock_codes: list, force_refresh=False) -> {code: PriceData.to_dict()}`。
- **价格字段名陷阱**：`PriceData.to_dict()`（`app/services/unified_stock_data.py:39-55`）的键是 **`price`**（不是 `current_price`），还有 `name`/`change_pct`。但 data-architecture.md 规则警告内存缓存层可能用 `current_price`。**因此读价必须防御：`data.get('price') or data.get('current_price')`**，且名称一律优先用 yaml 的 `stock_name`（yfinance 兜底会把 name 返回成 code）。
- 模板 extends `base.html`；导航在 `app/templates/base.html:24-33`，最后一个 `nav-link` 是 `supply_chain.index`。
- HTML 路由测试必须走 `create_app()`（base.html 跨 blueprint `url_for` 否则抛 BuildError），并设 `SCHEDULER_ENABLED=0`、`PYTHONIOENCODING=utf-8`。
- 测试平铺在 `tests/test_*.py`。

## File Structure

- Create `app/routes/valuations.py` — 路由 + `load_valuations()` + `compute_margin()` + `_extract_price()`，单一职责：估值页的数据装配与渲染。
- Modify `app/routes/__init__.py` — 加 `valuations_bp` 定义与 import。
- Modify `app/__init__.py` — `register_blueprint(valuations_bp)`。
- Create `app/templates/valuations.html` — 表格 + 刷新按钮 + JS。
- Modify `app/templates/base.html` — 导航加「估值汇总」入口。
- Create `docs/stock-analytics/valuations.yaml` — 数据源（先放最小种子，后由 agent 全量填充）。
- Create `tests/test_valuations.py` — 纯函数单测 + 路由 smoke + API monkeypatch。
- Create（临时，不入库）`scripts/_extract_valuations.py` — 一次性抽取脚本，跑完 `rm`。

---

### Task 1: 纯函数 `compute_margin` 与 `_extract_price`

**Files:**
- Create: `app/routes/valuations.py`
- Test: `tests/test_valuations.py`

- [ ] **Step 1: Write the failing test**

写入 `tests/test_valuations.py`：

```python
"""估值汇总页测试"""
import pytest

from app.routes.valuations import compute_margin, _extract_price


def test_compute_margin_normal():
    # base 7.78 / price 17.90 - 1 ≈ -0.5654（高估）
    assert compute_margin(7.78, 17.90) == pytest.approx(7.78 / 17.90 - 1)


def test_compute_margin_upside():
    assert compute_margin(20.0, 10.0) == pytest.approx(1.0)


def test_compute_margin_price_none():
    assert compute_margin(7.78, None) is None


def test_compute_margin_price_zero():
    assert compute_margin(7.78, 0) is None


def test_compute_margin_value_none():
    assert compute_margin(None, 17.90) is None


def test_extract_price_prefers_price_key():
    assert _extract_price({'price': 17.9, 'current_price': 99}) == 17.9


def test_extract_price_falls_back_to_current_price():
    assert _extract_price({'current_price': 12.3}) == 12.3


def test_extract_price_missing():
    assert _extract_price({}) is None


def test_extract_price_zero_is_none():
    # price=0 视为无效（API 偶发返回 0）
    assert _extract_price({'price': 0}) is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -v`
Expected: FAIL — `ModuleNotFoundError` / `ImportError: cannot import name 'compute_margin'`。

- [ ] **Step 3: Write minimal implementation**

创建 `app/routes/valuations.py`：

```python
from pathlib import Path
from typing import Any, Optional

import yaml
from flask import render_template, jsonify, request

from app.routes import valuations_bp
from app.services import unified_stock_data_service

VALUATIONS_PATH = Path(__file__).resolve().parents[2] / 'docs' / 'stock-analytics' / 'valuations.yaml'


def _extract_price(data: dict) -> Optional[float]:
    """防御读价：PriceData 用 'price'，内存缓存层可能用 'current_price'；0/None 均视为无效。"""
    price = data.get('price')
    if price is None:
        price = data.get('current_price')
    if not price:  # None 或 0
        return None
    return float(price)


def compute_margin(value: Optional[float], price: Optional[float]) -> Optional[float]:
    """安全边际 = value / price - 1（正=上行空间，负=高估）。value 缺或 price 无效返回 None。"""
    if value is None or not price:
        return None
    return value / price - 1
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -v`
Expected: PASS（9 passed）。

- [ ] **Step 5: Commit**

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat: 估值页纯函数 compute_margin/_extract_price"
```

---

### Task 2: `load_valuations()` 读取 yaml

**Files:**
- Modify: `app/routes/valuations.py`
- Test: `tests/test_valuations.py`

- [ ] **Step 1: Write the failing test**

在 `tests/test_valuations.py` 末尾追加：

```python
def test_load_valuations_parses(tmp_path):
    from app.routes.valuations import load_valuations
    p = tmp_path / 'valuations.yaml'
    p.write_text(
        "- stock_code: '000878'\n"
        "  stock_name: 云南铜业\n"
        "  market: A\n"
        "  currency: CNY\n"
        "  rating: watch\n"
        "  bear: 6.50\n"
        "  base: 7.78\n"
        "  bull: 8.87\n"
        "  conviction_date: '2026-06-02'\n"
        "  source_doc: sectors/materials/nonferrous/2026-06-02-云南铜业-buffett分析.md\n",
        encoding='utf-8',
    )
    rows = load_valuations(p)
    assert len(rows) == 1
    assert rows[0]['stock_code'] == '000878'
    assert rows[0]['base'] == 7.78
    assert rows[0]['stock_name'] == '云南铜业'


def test_load_valuations_missing_returns_empty(tmp_path):
    from app.routes.valuations import load_valuations
    assert load_valuations(tmp_path / 'nope.yaml') == []


def test_load_valuations_empty_file_returns_empty(tmp_path):
    from app.routes.valuations import load_valuations
    p = tmp_path / 'empty.yaml'
    p.write_text('', encoding='utf-8')
    assert load_valuations(p) == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -k load_valuations -v`
Expected: FAIL — `ImportError: cannot import name 'load_valuations'`。

- [ ] **Step 3: Write minimal implementation**

在 `app/routes/valuations.py` 的 `compute_margin` 之后追加：

```python
def load_valuations(path: Path = VALUATIONS_PATH) -> list[dict]:
    """读 valuations.yaml，返回 list[dict]；文件缺失/空返回 []。"""
    path = Path(path)
    if not path.exists():
        return []
    data = yaml.safe_load(path.read_text(encoding='utf-8'))
    if not isinstance(data, list):
        return []
    return data
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -k load_valuations -v`
Expected: PASS（3 passed）。

- [ ] **Step 5: Commit**

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat: 估值页 load_valuations 读取 yaml"
```

---

### Task 3: blueprint 注册 + 最小种子数据 + `GET /valuations/` smoke

**Files:**
- Modify: `app/routes/__init__.py`
- Modify: `app/__init__.py:254-271`
- Create: `docs/stock-analytics/valuations.yaml`（最小种子，Task 7 由 agent 全量替换）
- Modify: `app/routes/valuations.py`
- Test: `tests/test_valuations.py`

- [ ] **Step 1: Write the failing test**

在 `tests/test_valuations.py` 末尾追加：

```python
@pytest.fixture
def app_client(monkeypatch):
    import os
    os.environ['SCHEDULER_ENABLED'] = '0'
    from app import create_app
    from app.services import unified_stock_data_service
    # 避免真实联网：返回空价（页面应仍 200，价显示 —）
    monkeypatch.setattr(unified_stock_data_service, 'get_realtime_prices',
                        lambda codes, force_refresh=False: {})
    app = create_app()
    app.config['TESTING'] = True
    with app.test_client() as client:
        yield client


def test_index_route_smoke(app_client):
    resp = app_client.get('/valuations/')
    assert resp.status_code == 200
    assert '估值'.encode('utf-8') in resp.data
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py::test_index_route_smoke -v`
Expected: FAIL — `werkzeug.routing.BuildError` 或 404（`valuations_bp` 未注册 / 模板缺失）。

- [ ] **Step 3a: 定义 blueprint**

修改 `app/routes/__init__.py`：在第 20 行 `supply_chain_bp = ...` 之后加：

```python
valuations_bp = Blueprint('valuations', __name__, url_prefix='/valuations')
```

并在第 22 行 import 行末尾追加 `, valuations`：

```python
from app.routes import main, position, advice, category, trade, stock, daily_record, profit, rebalance, heavy_metals, alert, briefing, stock_detail, watch, news, value_dip, earnings_page, supply_chain, valuations
```

- [ ] **Step 3b: 注册 blueprint**

修改 `app/__init__.py`：在第 271 行 `app.register_blueprint(supply_chain_bp)` 之后加（同时确保 import 行包含 `valuations_bp`——该文件顶部从 `app.routes` import 各 bp，按现有写法把 `valuations_bp` 加入 import 列表）：

```python
    app.register_blueprint(valuations_bp)
```

- [ ] **Step 3c: 最小种子 yaml**

创建 `docs/stock-analytics/valuations.yaml`（先放 1 条真实数据，Task 7 全量替换）：

```yaml
- stock_code: '000878'
  stock_name: 云南铜业
  market: A
  currency: CNY
  rating: watch
  bear: 6.50
  base: 7.78
  bull: 8.87
  conviction_date: '2026-06-02'
  source_doc: sectors/materials/nonferrous/2026-06-02-云南铜业-buffett分析.md
  note: 三情景加权 base 7.78
```

- [ ] **Step 3d: 实现 index 路由**

在 `app/routes/valuations.py` 末尾追加：

```python
def _enrich(rows: list[dict], prices: dict) -> list[dict]:
    out = []
    for r in rows:
        data = prices.get(r['stock_code']) or {}
        price = _extract_price(data)
        out.append({
            **r,
            'current_price': price,
            'margin_bear': compute_margin(r.get('bear'), price),
            'margin_base': compute_margin(r.get('base'), price),
            'margin_bull': compute_margin(r.get('bull'), price),
        })
    # Base 安全边际降序（最低估在上）；缺值沉底
    out.sort(key=lambda x: (x['margin_base'] is None, -(x['margin_base'] or 0)))
    return out


@valuations_bp.route('/')
def index():
    rows = load_valuations()
    codes = [r['stock_code'] for r in rows]
    prices = unified_stock_data_service.get_realtime_prices(codes) if codes else {}
    return render_template('valuations.html', rows=_enrich(rows, prices))
```

- [ ] **Step 3e: 最小模板（占位，Task 5 完善）**

创建 `app/templates/valuations.html`：

```html
{% extends "base.html" %}
{% block title %}估值汇总 - 股票管理{% endblock %}
{% block content %}
<h4>估值汇总</h4>
<table class="table"><tbody>
{% for r in rows %}
<tr><td>{{ r.stock_code }}</td><td>{{ r.stock_name }}</td></tr>
{% endfor %}
</tbody></table>
{% endblock %}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py::test_index_route_smoke -v`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
rtk git add app/routes/__init__.py app/__init__.py app/routes/valuations.py app/templates/valuations.html docs/stock-analytics/valuations.yaml tests/test_valuations.py && rtk git commit -m "feat: 注册 valuations 路由与种子数据，GET / smoke 通过"
```

---

### Task 4: `GET /valuations/api/prices` 批量拉价 JSON

**Files:**
- Modify: `app/routes/valuations.py`
- Test: `tests/test_valuations.py`

- [ ] **Step 1: Write the failing test**

在 `tests/test_valuations.py` 末尾追加：

```python
def test_api_prices_structure(app_client, monkeypatch):
    from app.services import unified_stock_data_service

    def fake_prices(codes, force_refresh=False):
        assert force_refresh is True  # ?force=1 必须透传
        return {'000878': {'price': 17.90, 'name': '云南铜业'}}

    monkeypatch.setattr(unified_stock_data_service, 'get_realtime_prices', fake_prices)
    resp = app_client.get('/valuations/api/prices?force=1')
    assert resp.status_code == 200
    body = resp.get_json()
    assert '000878' in body
    row = body['000878']
    assert row['current_price'] == 17.90
    # base 7.78 / 17.90 - 1 ≈ -0.5654
    assert row['margin_base'] == pytest.approx(7.78 / 17.90 - 1)


def test_api_prices_missing_price_yields_none(app_client, monkeypatch):
    from app.services import unified_stock_data_service
    monkeypatch.setattr(unified_stock_data_service, 'get_realtime_prices',
                        lambda codes, force_refresh=False: {'000878': {'price': 0}})
    resp = app_client.get('/valuations/api/prices?force=1')
    row = resp.get_json()['000878']
    assert row['current_price'] is None
    assert row['margin_base'] is None
```

注意：`app_client` fixture 已 monkeypatch 过 `get_realtime_prices`；本测试在用例内二次 monkeypatch 覆盖它，pytest 的 monkeypatch 顺序保证用例内的设置生效。

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -k api_prices -v`
Expected: FAIL — 404（`/api/prices` 未定义）。

- [ ] **Step 3: Write minimal implementation**

在 `app/routes/valuations.py` 末尾追加：

```python
@valuations_bp.route('/api/prices')
def api_prices():
    force = request.args.get('force') == '1'
    rows = load_valuations()
    codes = [r['stock_code'] for r in rows]
    prices = unified_stock_data_service.get_realtime_prices(codes, force_refresh=force) if codes else {}
    out = {}
    for r in rows:
        data = prices.get(r['stock_code']) or {}
        price = _extract_price(data)
        out[r['stock_code']] = {
            'current_price': price,
            'margin_bear': compute_margin(r.get('bear'), price),
            'margin_base': compute_margin(r.get('base'), price),
            'margin_bull': compute_margin(r.get('bull'), price),
        }
    return jsonify(out)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -k api_prices -v`
Expected: PASS（2 passed）。

- [ ] **Step 5: Commit**

```bash
rtk git add app/routes/valuations.py tests/test_valuations.py && rtk git commit -m "feat: 估值页 /api/prices 批量拉价 JSON"
```

---

### Task 5: 完整模板（表格 + 刷新按钮 + JS）+ 导航入口

**Files:**
- Modify: `app/templates/valuations.html`
- Modify: `app/templates/base.html:32`（在 supply_chain 入口后加）
- Test: `tests/test_valuations.py`（沿用 smoke，断言新增列头）

- [ ] **Step 1: Write the failing test**

在 `tests/test_valuations.py` 末尾追加：

```python
def test_index_has_table_headers_and_refresh(app_client):
    resp = app_client.get('/valuations/')
    html = resp.data.decode('utf-8')
    for col in ('Bear', 'Base', 'Bull', '当前价', '安全边际'):
        assert col in html, f'缺列头 {col}'
    assert 'id="refresh-btn"' in html
```

- [ ] **Step 2: Run test to verify it fails**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py::test_index_has_table_headers_and_refresh -v`
Expected: FAIL — 占位模板无这些列头/按钮。

- [ ] **Step 3a: 完整模板**

覆盖 `app/templates/valuations.html`：

```html
{% extends "base.html" %}
{% block title %}估值汇总 - 股票管理{% endblock %}
{% block extra_css %}
<style>
.margin-pos { color: #198754; font-weight: 600; }
.margin-neg { color: #dc3545; font-weight: 600; }
.val-table th, .val-table td { font-size: 13px; white-space: nowrap; vertical-align: middle; }
.rating-badge { font-size: 11px; }
</style>
{% endblock %}
{% block content %}
<div class="d-flex justify-content-between align-items-center mb-3">
  <h4 class="mb-0"><i class="bi bi-cash-stack"></i> 估值汇总</h4>
  <div>
    <span id="refresh-status" class="text-muted small me-2"></span>
    <button id="refresh-btn" class="btn btn-sm btn-primary" onclick="refreshPrices()">🔄 刷新实时价</button>
  </div>
</div>
{% if not rows %}
<div class="alert alert-warning">暂无估值数据（docs/stock-analytics/valuations.yaml 为空或缺失）。</div>
{% else %}
<div class="table-responsive">
<table class="table table-sm table-hover val-table">
  <thead class="table-light">
    <tr>
      <th>代码</th><th>名称</th><th>板块</th><th>评级</th>
      <th class="text-end">Bear</th><th class="text-end">Base</th><th class="text-end">Bull</th>
      <th class="text-end">当前价</th><th class="text-end">Base安全边际</th>
      <th>日期</th><th>文档</th>
    </tr>
  </thead>
  <tbody>
  {% for r in rows %}
    <tr data-code="{{ r.stock_code }}">
      <td>{{ r.stock_code }}</td>
      <td>{{ r.stock_name }}</td>
      <td>{{ r.sector or '—' }}</td>
      <td><span class="badge bg-secondary rating-badge">{{ r.rating or '—' }}</span></td>
      <td class="text-end">{{ '%.2f'|format(r.bear) if r.bear is not none else '—' }}</td>
      <td class="text-end">{{ '%.2f'|format(r.base) if r.base is not none else '—' }}</td>
      <td class="text-end">{{ '%.2f'|format(r.bull) if r.bull is not none else '—' }}</td>
      <td class="text-end cell-price">{{ '%.2f'|format(r.current_price) if r.current_price is not none else '—' }}</td>
      <td class="text-end cell-margin {% if r.margin_base is not none and r.margin_base >= 0 %}margin-pos{% elif r.margin_base is not none %}margin-neg{% endif %}">
        {{ '%+.1f%%'|format(r.margin_base * 100) if r.margin_base is not none else '—' }}
      </td>
      <td>{{ r.conviction_date or '—' }}</td>
      <td>{% if r.source_doc %}<a href="https://github.com" onclick="return false" title="{{ r.source_doc }}">📄</a>{% else %}—{% endif %}</td>
    </tr>
  {% endfor %}
  </tbody>
</table>
</div>
{% endif %}

<script>
function fmtPrice(v) { return v === null || v === undefined ? '—' : v.toFixed(2); }
function fmtMargin(v) { return v === null || v === undefined ? '—' : (v >= 0 ? '+' : '') + (v * 100).toFixed(1) + '%'; }

async function refreshPrices() {
  const btn = document.getElementById('refresh-btn');
  const status = document.getElementById('refresh-status');
  btn.disabled = true; status.textContent = '刷新中…';
  try {
    const resp = await fetch('/valuations/api/prices?force=1');
    if (!resp.ok) throw new Error('http ' + resp.status);
    const data = await resp.json();
    document.querySelectorAll('tr[data-code]').forEach(tr => {
      const row = data[tr.dataset.code];
      if (!row) return;
      tr.querySelector('.cell-price').textContent = fmtPrice(row.current_price);
      const m = tr.querySelector('.cell-margin');
      m.textContent = fmtMargin(row.margin_base);
      m.classList.remove('margin-pos', 'margin-neg');
      if (row.margin_base !== null && row.margin_base !== undefined) {
        m.classList.add(row.margin_base >= 0 ? 'margin-pos' : 'margin-neg');
      }
    });
    status.textContent = '已更新 ' + new Date().toLocaleTimeString();
  } catch (e) {
    status.textContent = '刷新失败';
  } finally {
    btn.disabled = false;
  }
}
</script>
{% endblock %}
```

- [ ] **Step 3b: 导航入口**

修改 `app/templates/base.html`，在第 32 行 `supply_chain.index` 的 `<a>` 之后加一行：

```html
                <a class="nav-link" href="{{ url_for('valuations.index') }}">估值汇总</a>
```

- [ ] **Step 4: Run test to verify it passes**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -v`
Expected: PASS（全部用例，含 smoke 与列头断言）。

- [ ] **Step 5: Commit**

```bash
rtk git add app/templates/valuations.html app/templates/base.html tests/test_valuations.py && rtk git commit -m "feat: 估值页完整表格+刷新按钮与导航入口"
```

---

### Task 6: 全量回归 + 手动启动核验

**Files:** 无（验证步骤）

- [ ] **Step 1: 跑全套估值测试**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -v`
Expected: 全 PASS。

- [ ] **Step 2: 手动启动核验（人工）**

Run: `python run.py`，浏览器开 `http://127.0.0.1:5000/valuations/`。
Expected：看到表格（含种子的云南铜业行）；点「🔄 刷新实时价」后当前价与 Base安全边际单元格更新、右上显示「已更新 HH:MM:SS」。

- [ ] **Step 3: Commit（如有微调）**

```bash
rtk git add -A && rtk git commit -m "chore: 估值页核验微调"
```

---

### Task 7: Agent 一次性抽取存量 buffett 文档填充 valuations.yaml

> 本任务由 agent 执行抽取，产物是 `docs/stock-analytics/valuations.yaml` 全量数据。抽取脚本 `scripts/_extract_valuations.py` 为一次性脚本，**跑完即 `rm`、不入库**（符合 dev-conventions「一次性脚本不入库」）。

**Files:**
- 临时 Create: `scripts/_extract_valuations.py`（跑完删除）
- Modify（数据全量替换）: `docs/stock-analytics/valuations.yaml`

- [ ] **Step 1: 列出候选文档**

用 Glob 收集：
- `docs/stock-analytics/sectors/**/*buffett分析.md`
- `docs/stock-analytics/cross-sector/**/*buffett*.md`（一档含 2 只股，各抽一条）

- [ ] **Step 2: 逐篇抽取三档每股估值**

对每篇，读 frontmatter（`stock_code`/`stock_name`/`sector`/`rating`/`conviction_date`）与正文结论摘要段，抽出：
- `bear` / `base` / `bull`：**每股原币内在价值**（元/股 或 USD/HKD）。文档若只给总市值（亿）+ 总股本，换算成每股 = 总市值(亿)×1e8 / 股本(股)，或直接取文中已给的「X 元/股」。
- `market`：由 `stock_code` 经 `MarketIdentifier.identify(code)` 判（A/US/HK）。
- `currency`：A→CNY，US→USD，HK→HKD。
- `source_doc`：相对 `docs/stock-analytics/` 的路径。
- 缺某档 → 该字段写 `null`。

抽取要点（参考 `sectors/materials/nonferrous/2026-06-02-云南铜业-buffett分析.md` 结论摘要）：文中常写「base 7.78 元/股」「bull 情景 215 亿 / 8.87 元」「bear …」。优先取每股值。

- [ ] **Step 3: 去重取最新**

同一 `stock_code` 多篇 → 按 `conviction_date`（字符串比较前先 `str(...)`，YAML 可能解析成 date 对象）取最新一篇，其余丢弃。

- [ ] **Step 4: 写出 yaml**

`scripts/_extract_valuations.py` 用 `yaml.safe_dump(rows, allow_unicode=True, sort_keys=False)` 写 `docs/stock-analytics/valuations.yaml`，`encoding='utf-8'`。`stock_code` 必须带引号（防前导 0 丢失）——dump 时确保为 str 类型即可，或写出后核查。脚本顶部加 `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))`（若 import app 模块）。

- [ ] **Step 5: 核验 + 删除脚本**

Run: `rtk PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_valuations.py -v`（确保新数据不破坏 load/route）。
人工抽查 yaml 几条 `base` 与源文档一致；确认 `stock_code` 带引号、前导 0 未丢。
然后 `rm scripts/_extract_valuations.py`。

- [ ] **Step 6: Commit**

```bash
rtk git add docs/stock-analytics/valuations.yaml && rtk git commit -m "data: 全量填充 valuations.yaml（存量 buffett 三档估值抽取）"
```

注意（dev-conventions 并发 git）：`git add` 与 `git commit` 同一命令链；只精确 add `valuations.yaml`，勿 `git add -A` 裹挟他人在写文档。

---

## 自检结论

- **Spec 覆盖**：①数据文件→Task 3 种子 + Task 7 全量；②初始抽取→Task 7；③Flask 路由→Task 3/4；④安全边际→Task 1；⑤前端模板→Task 5；⑥错误边界（price None/0、缺档、空 yaml、刷新失败）→Task 1/2/4/5 均有用例或处理；⑦测试→Task 1-5 含纯函数/路由 smoke/API monkeypatch。全部有对应任务。
- **价格字段陷阱**已在「已核实事实」与 `_extract_price` 显式处理（`price` 优先、`current_price` 兜底、0/None 归 None）。
- **命名一致性**：`load_valuations`/`compute_margin`/`_extract_price`/`_enrich`/`api_prices`/`valuations_bp` 全程一致；margin 键 `margin_bear/base/bull` 在路由、API、JS、模板四处一致。
