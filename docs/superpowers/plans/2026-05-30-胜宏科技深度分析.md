# 胜宏科技（300476）Buffett 深度分析 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建一篇胜宏科技（300476.SZ）的 buffett 个股深度分析文档，整合仓库已有 3 篇散点，补齐六维深度（市场规模/盈利能力/全球竞争力/核心优势/Rubin 潜力/AI 潜力），并以多情景估值重开估值问题、由结果反填 rating。

**Architecture:** 先用一次性脚本 + web 研究采集数据（实时价、财务时序、主营构成、全球同业、Rubin/AI 敞口），再按仓库 buffett 范式（对标 `沃尔核材-buffett分析.md`）分节撰写 Markdown，最后用 `lint_docs_frontmatter.py` / `lint_docs_refs.py` 验证并补全 related_docs 反向对称。分析视角调用 `buffett` 技能。

**Tech Stack:** Markdown + YAML frontmatter；数据：腾讯 `qt.gtimg.cn` HTTP / akshare（`stock_financial_abstract_ths` / `stock_zh_valuation_baidu` / `stock_zygc_em`）/ WebSearch+WebFetch；校验：仓库 `scripts/lint_docs_*.py`。

**Spec:** `docs/superpowers/specs/2026-05-30-胜宏科技深度分析-design.md`

**关键约定（来自 CLAUDE.md / rules）：**
- 响应中文，不写多余注释，不写 backup 文件。
- 所有 git 命令前加 `rtk`，链式 `&&` 中也要。
- 一次性脚本 `scripts/_xxx.py` 顶部加 `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))`；任务结束 `rm`，不入库。
- 写含中文文件必须 `encoding='utf-8'`；打印 emoji 用 `PYTHONIOENCODING=utf-8`。
- 实时价直连 HTTP（不走 create_app）；`stock_code` frontmatter 必须带引号 `'300476'`。
- 所有份额/良率/ASP/TAM 标「非披露口径，量级参考」。

---

## File Structure

| 文件 | 职责 | 动作 |
|------|------|------|
| `scripts/_shenghong_data.py` | 一次性采集实时价+财务+主营构成，输出 JSON | 创建→运行→任务末 rm |
| `.omc/artifacts/shenghong_data.json` | 采集产物（已 gitignore） | 脚本写出 |
| `.omc/research/shenghong_web_notes.md` | web 研究笔记（全球同业/Rubin/AI 敞口，含来源） | 创建 |
| `docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md` | **主交付物** | 创建 |
| `docs/stock-analytics/comps/2026-05-29-沪电-胜宏-生益-市值重估-comps.md` | 补反向 related_docs 条目 | 修改 frontmatter |
| `docs/stock-analytics/comps/2026-05-22-胜宏-沪电-comps-良率与Rubin份额.md` | 补反向 related_docs 条目 | 修改 frontmatter |
| `docs/stock-analytics/quarterly/26q1/2026-04-29-胜宏科技-26Q1季报点评.md` | 补反向 related_docs 条目 | 修改 frontmatter |

---

## Task 1: 采集硬数据（实时价 + 财务时序 + 主营构成）

**Files:**
- Create: `scripts/_shenghong_data.py`
- Output: `.omc/artifacts/shenghong_data.json`

- [ ] **Step 1: 写采集脚本**

```python
# scripts/_shenghong_data.py
import sys, json, urllib.request
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

out = {}

# 1) 实时价（腾讯 HTTP，GBK，~分隔）
req = urllib.request.Request("http://qt.gtimg.cn/q=sz300476",
                             headers={"User-Agent": "Mozilla/5.0"})
raw = urllib.request.urlopen(req, timeout=10).read().decode("gbk", "ignore")
f = raw.split("~")
out["realtime"] = {
    "name": f[1], "price": f[3], "prev_close": f[4], "open": f[5],
    "change_pct": f[32], "PE_TTM": f[39], "amplitude": f[43] if len(f) > 43 else None,
    "mktcap_yi": f[45], "PB": f[46],
}

# 2) 多年财务时序
import akshare as ak
try:
    df = ak.stock_financial_abstract_ths(symbol="300476", indicator="按年度")
    out["financial_abstract"] = df.to_dict(orient="records")
except Exception as e:
    out["financial_abstract_error"] = f"{type(e).__name__}: {e}"

# 3) PE/PB 5y 分位
for ind, key in [("市盈率(TTM)", "pe_baidu"), ("市净率", "pb_baidu")]:
    try:
        v = ak.stock_zh_valuation_baidu(symbol="300476", indicator=ind, period="近5年")
        out[key] = v.tail(3).to_dict(orient="records")
        out[key + "_describe"] = v[v.columns[1]].describe().to_dict()
    except Exception as e:
        out[key + "_error"] = f"{type(e).__name__}: {e}"

# 4) 主营构成（最新报告期）
try:
    z = ak.stock_zygc_em(symbol="SZ300476")
    latest = z[z["报告日期"] == z["报告日期"].max()]
    out["zygc_latest"] = latest.to_dict(orient="records")
except Exception as e:
    out["zygc_error"] = f"{type(e).__name__}: {e}"

dst = Path(__file__).resolve().parent.parent / ".omc" / "artifacts"
dst.mkdir(parents=True, exist_ok=True)
(dst / "shenghong_data.json").write_text(
    json.dumps(out, ensure_ascii=False, indent=2, default=str), encoding="utf-8")
print("OK realtime:", out["realtime"]["name"], out["realtime"]["price"],
      "mktcap_yi=", out["realtime"]["mktcap_yi"])
```

- [ ] **Step 2: 运行脚本**

Run: `PYTHONIOENCODING=utf-8 python scripts/_shenghong_data.py`
Expected: 打印 `OK realtime: 胜宏科技 <价格> mktcap_yi= <市值>`；`.omc/artifacts/shenghong_data.json` 生成。
若 akshare 某接口报错（RemoteDisconnected/AttributeError），JSON 里会有 `*_error` 字段——记录下来，撰写时该项标「接口不可用，沿用仓库已有口径」。

- [ ] **Step 3: 读取并核对 JSON**

用 Read 工具读 `.omc/artifacts/shenghong_data.json`。核对：① 实时价/市值与 5-29 comps 的 368.52/3622亿 量级一致（5-30 当日可能变动）；② 财务时序含 ROE/毛利率/营收/净利多年；③ 主营构成能看出 PCB 占比与海外敞口。
Expected: 拿到今日实时锚 + 多年财务序列 + 主营拆分。

> 本任务无 git 提交（采集产物在 gitignore 的 `.omc/artifacts/`）。

---

## Task 2: Web 研究（全球竞争力 + Rubin 潜力 + AI 潜力）

**Files:**
- Create: `.omc/research/shenghong_web_notes.md`

- [ ] **Step 1: 搜全球同业对标**

用 WebSearch 检索（中英各一轮，交叉验证 2–3 源）：
- `臻鼎科技 欣兴电子 健鼎 PCB 2025 营收 毛利率 AI` / `global PCB ranking 2025 revenue Zhen Ding Unimicron`
- `Ibiden ABF substrate AI 营收` / `胜宏科技 全球 PCB 排名 2025`
目标抓取：臻鼎(鹏鼎)、欣兴 Unimicron、健鼎 Tripod、华通 Compeq、日系 Ibiden 的**营收量级 / 毛利率 / AI 数据中心敞口 / 全球 PCB 营收排名**，以及胜宏的全球排名位置。

- [ ] **Step 2: 搜 Rubin 潜力**

WebSearch：`NVIDIA Rubin VR200 量产 时间表 2026 2027` / `Rubin GPU PCB ASP 单板价值量 胜宏 份额`。
目标：Rubin/Rubin Ultra 量产时间表（对照仓库 5-22 的 CY Q3 2026 量产/Q4 爬坡/Q1 2027 出货）、GPU 算力板单板 ASP、胜宏份额可持续性证据、立讯/景旺/鹏鼎切入威胁。

- [ ] **Step 3: 搜非 NVIDIA AI 敞口**

WebSearch：`胜宏科技 谷歌 TPU Sokol 订单` / `胜宏 AMD MI 博通 Marvell ASIC PCB` / `Victory Giant non-NVIDIA AI customers`。
目标：Google TPU V7/V8、AMD MI 系列、博通/Marvell ASIC、北美云厂自研芯片的配套进展，判断客户多元化真实度。

- [ ] **Step 4: 写研究笔记**

把抓到的关键数字 + **每条标注来源链接 + 口径**写入 `.omc/research/shenghong_web_notes.md`（用 `encoding='utf-8'`）。冲突数据并列保留、标量级。
Expected: 三个新维度都有可引用的多源数据，含来源与「量级参考」标注。

> 本任务无 git 提交（研究笔记在 `.omc/research/`）。

---

## Task 3: 创建文档骨架 + frontmatter（lint 通过）

**Files:**
- Create: `docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md`

- [ ] **Step 1: 调用 buffett 技能建立分析视角**

调用 `/buffett`（Skill: buffett）激活完整投资思维系统，作为后续撰写的分析框架。

- [ ] **Step 2: 写 frontmatter + 12 节空骨架**

用 Write 创建文件（`encoding='utf-8'` 由 Write 工具保证）。frontmatter 按 spec：

```yaml
---
doc_type: buffett
stock_code: '300476'
stock_name: 胜宏科技
sector: semiconductor
subsector: pcb
themes:
  - AI-PCB
  - Rubin
  - HDI
  - 正交背板
  - NVIDIA链
  - 全球竞争力
rating: watch
watch_reason: 起草占位——估值后由多情景安全边际结果反填
conviction_date: '2026-05-30'
thesis: 起草占位——估值后定稿
related_docs:
  - path: ../../../comps/2026-05-29-沪电-胜宏-生益-市值重估-comps.md
    note: 三家市值重估横比（本篇胜宏纵深的横向坐标）
    symmetric: true
  - path: ../../../comps/2026-05-22-胜宏-沪电-comps-良率与Rubin份额.md
    note: 良率 92% + Rubin GPU 板 50% 份额基线
    symmetric: true
  - path: ../../../quarterly/26q1/2026-04-29-胜宏科技-26Q1季报点评.md
    note: 26Q1 兑现度（加速度结束 + OCF/EPS 1.64x 验真）
    symmetric: true
---
# 胜宏科技（300476）Buffett 分析

> 撰写日期：2026-05-30 ｜ 框架：Warren Buffett Investment Thinking System
> 数据口径：实时锚 2026-05-30（腾讯 qt.gtimg.cn）；财务时序 stock_financial_abstract_ths；主营构成 stock_zygc_em；份额/良率/ASP 为券商/媒体口径，量级参考，不构成披露事实

## 结论
## 能力圈评估
## 关键假设（决策依赖，待验证）
## 业务质量
## 财务快照
## 六维深度（市场规模 / 盈利能力 / 全球竞争力 / 核心优势 / Rubin 潜力 / AI 潜力）
## 估值（重开 · 多情景）
## 卖出标准逐项核对
## 关键风险
## 监控指标
## 总体评估
## 免责声明
```

- [ ] **Step 3: 跑 frontmatter lint**

Run: `python scripts/lint_docs_frontmatter.py`
Expected: 退出码 0（watch_reason 已填占位非空，校验通过）。若报错按提示补字段。

- [ ] **Step 4: 提交骨架**

```bash
rtk git add "docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md" && rtk git commit -m "docs(胜宏): buffett 深度分析骨架 + frontmatter"
```

---

## Task 4: 撰写第 2–5 节（能力圈 / 关键假设 / 业务质量 / 财务快照）

**Files:**
- Modify: `docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md`

- [ ] **Step 1: 能力圈评估**

一句话商业模式：胜宏是「6 阶 24 层 HDI + 78 层正交背板的高多层 PCB 制造龙头，GPU 算力主板卡级配套」。讲清「卖高良率制造」与「绑定 NVIDIA 单客户」两个本质。

- [ ] **Step 2: 关键假设（4–5 条，对照 Task 1/2 数据）**

写成编号列表：① Rubin GPU 板 ~50% 份额守稳；② 92% 良率接力期（GB300→Rubin）可持续；③ NVIDIA >50% 集中度不恶化；④ 惠州四期/泰国一期产能爬坡兑现；⑤ 非 NVIDIA AI 敞口能否打开（来自 Task 2）。每条标「待验证」。

- [ ] **Step 3: 业务质量**

护城河（92% 良率 vs 行业 50–60% + GPU 算力板 ~50% 份额 + 78 层正交背板独家，定护城河强度）/ 管理层（注明「诚信未深度尽调，仅财报层面」）/ 商业模式（单客户 franchise 的「借来的护城河」脆弱性）/ 机构强制症预警。

- [ ] **Step 4: 财务快照**

用 Task 1 的财务时序做表：ROE（近 5 年）/ 毛利率 / 净利率 / OCF·EPS / OCF/EPS / 资产负债率 / 5y 净利 CAGR（+52.7%）。加 worst-case 测试（营收 -30% 偿债能力）。数字以 Task 1 JSON 为准，与 4-29 季报（26Q1 营收 55.19亿/净利 12.88亿/毛利率 34.46%/OCF·EPS 2.43/单季 ROE 7.62%）对齐。

- [ ] **Step 5: 提交**

```bash
rtk git add "docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md" && rtk git commit -m "docs(胜宏): 能力圈/关键假设/业务质量/财务快照"
```

---

## Task 5: 撰写第 6 节「六维深度」（核心）

**Files:**
- Modify: `docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md`

- [ ] **Step 1: ① 市场规模**

AI/数据中心服务器 PCB TAM + 高多层/HDI 细分（沿用 5-29 comps：AI-PCB ~$9.7B Mordor / GPU+ASIC 口径 ~$12.6B 国内券商）+ Task 2 最新增速口径。标量级与多源。

- [ ] **Step 2: ② 盈利能力**

毛利率序 vs 净利率序（胜宏净利率 23.3% 同业第一）、OCF/EPS 1.64x、5y 净利 CAGR +52.7%、与全球同业净利率对标（用 Task 2 的台系/日系数据）。

- [ ] **Step 3: ③ 全球竞争力（内联对标表）**

Markdown 表格：胜宏 vs 臻鼎(鹏鼎)/欣兴/健鼎/华通/Ibiden，列【营收量级 / 毛利率 / AI 数据中心敞口 / 技术层数 / 全球 PCB 营收排名】。数据来自 Task 2，每个非披露数标来源/量级。点明胜宏在 AI-PCB 子赛道的相对位置（份额 vs 整体规模）。

- [ ] **Step 4: ④ 核心优势**

92% 良率工艺壁垒（行业 50–60%）+ 78 层正交背板独家 + 产能扩张卡位（惠州四期 6 阶 HDI 2025-06 投产即超 80%、泰国一期）。

- [ ] **Step 5: ⑤ Rubin 潜力**

VR200/Rubin Ultra 时间表（CY Q3 2026 量产/Q4 爬坡/Q1 2027 出货）、GPU 算力板单板 ASP、份额可持续性、接力期良率风险、立讯/景旺/鹏鼎切入威胁。引用 5-22 comps 的「分工地图」+ Task 2 增量。

- [ ] **Step 6: ⑥ AI 潜力**

非 NVIDIA 敞口：Google TPU V7/V8（Sokol）、AMD MI、博通/Marvell ASIC、北美云自研芯片（Task 2 数据）；客户多元化辩证（NVIDIA 客户分散 ≠ 胜宏客户分散）。

- [ ] **Step 7: 提交**

```bash
rtk git add "docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md" && rtk git commit -m "docs(胜宏): 六维深度（市场规模/盈利/全球竞争力/核心优势/Rubin/AI）"
```

---

## Task 6: 撰写第 7 节「估值（多情景）」+ 反填 rating/thesis/watch_reason

**Files:**
- Modify: `docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md`

- [ ] **Step 1: 多情景估值**

以 Task 1 今日实时价/市值为锚，写牛/基准/熊三情景（沿用 4-29 季报情景法并更新到 5-30）：
- 基准：26 全年净利外推 + 给 PE 区间 → 公允市值
- 牛：Rubin 27/28 续航 + 非 NVIDIA 放量 → 上限
- 熊：毛利率回归 28% + NVIDIA 见顶 → 下限
加 **Rubin 27/28 续航敏感性表**（净利 × PE 矩阵）。给内在价值区间、当前安全边际、建议买入价。对照 5-29 基线（胜宏高估 +101%~+262%）。

- [ ] **Step 2: 反填 frontmatter 的 rating / watch_reason / thesis**

按 Step 1 安全边际结果定 rating：严重高估→`watch`（或 `exclude`）；有边际→`config`/`core`。把占位的 `watch_reason`（或换 `exclude_reason`）和 `thesis` 改写为与估值结论自洽的真实文案。
若改为 `exclude`，frontmatter 需把 `watch_reason` 换成 `exclude_reason`（schema 要求）。

- [ ] **Step 3: 跑 frontmatter lint 复验**

Run: `python scripts/lint_docs_frontmatter.py`
Expected: 退出码 0（rating 与对应 reason 字段自洽）。

- [ ] **Step 4: 提交**

```bash
rtk git add "docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md" && rtk git commit -m "docs(胜宏): 多情景估值 + rating/thesis 反填"
```

---

## Task 7: 撰写第 1、8–12 节（结论 / 卖出标准 / 风险 / 监控 / 总评 / 免责）

**Files:**
- Modify: `docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md`

- [ ] **Step 1: 结论**（回填，与 rating 一致）：评级 + 一句话立场。
- [ ] **Step 2: 卖出标准逐项核对**：4 项（价格高估/护城河/管理层诚信/更优机会，对照同业沪电/深南）。
- [ ] **Step 3: 关键风险（≤3）**：单一客户 NVIDIA、接力期良率、估值透支。
- [ ] **Step 4: 监控指标**：季度跟踪（毛利率/OCF/NVIDIA capex/份额）+ 触发买入 + 触发卖出。
- [ ] **Step 5: 总体评估**：Buffett 口吻收尾段落。
- [ ] **Step 6: 免责声明**：份额/良率/ASP/TAM 非披露口径标注。

- [ ] **Step 7: 提交**

```bash
rtk git add "docs/stock-analytics/sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md" && rtk git commit -m "docs(胜宏): 结论/卖出标准/风险/监控/总评/免责"
```

---

## Task 8: related_docs 反向对称 + 全量 lint

**Files:**
- Modify: 三篇目标文档的 frontmatter（补指回本篇的 related_docs 条目）
- Modify: 本篇（rewrite-blocks 重生顶部 markdown 块）

- [ ] **Step 1: 给三篇目标文档各加一条反向 related_docs**

在以下三篇的 frontmatter `related_docs:` 列表各加一条指向本篇、`symmetric: true`：
- `comps/2026-05-29-沪电-胜宏-生益-市值重估-comps.md`
- `comps/2026-05-22-胜宏-沪电-comps-良率与Rubin份额.md`
- `quarterly/26q1/2026-04-29-胜宏科技-26Q1季报点评.md`

路径用各自相对本篇的路径（如季报点评里写 `path: ../../sectors/semiconductor/pcb/2026-05-30-胜宏科技-buffett分析.md`，note 写「胜宏 buffett 个股深度分析（六维 + 多情景估值）」）。

- [ ] **Step 2: 重生所有顶部 markdown 块**

Run: `python scripts/lint_docs_refs.py --rewrite-blocks`
Expected: 自动生成/更新各文档 `<!-- BEGIN related_docs -->` 块。

- [ ] **Step 3: 全量校验**

Run: `python scripts/lint_docs_frontmatter.py && python scripts/lint_docs_refs.py`
Expected: 两条均退出码 0（无违例、反向对称满足）。

- [ ] **Step 4: 提交**

```bash
rtk git add docs/stock-analytics && rtk git commit -m "docs(胜宏): related_docs 反向对称 + 重生引用块"
```

---

## Task 9: 清理一次性脚本

- [ ] **Step 1: 删除采集脚本**

Run: `rm scripts/_shenghong_data.py`
（研究笔记 `.omc/research/` 与产物 `.omc/artifacts/` 已 gitignore，保留备查，无需删。）

- [ ] **Step 2: 确认工作区干净**

Run: `rtk git status`
Expected: 无 `scripts/_shenghong_data.py` 残留；主文档与三篇 related_docs 改动均已提交。

---

## 验收清单（对照 spec §6）

- [ ] frontmatter 通过 `lint_docs_frontmatter.py`（退出码 0）
- [ ] related_docs 通过 `lint_docs_refs.py` 且三篇目标反向对称已补
- [ ] 六维全部成节，全球竞争力含内联对标表
- [ ] 估值为多情景（牛/基准/熊）含 Rubin 敏感性，今日实时价锚定
- [ ] rating 与 watch_reason/exclude_reason/thesis 由估值结果反填、互相自洽
- [ ] 所有非披露口径数据已标注来源与「量级参考」
- [ ] 与现有 3 篇结论不矛盾（若变化，显式说明依据，对照 5-29 高估基线）
- [ ] 一次性脚本已 rm，工作区干净
