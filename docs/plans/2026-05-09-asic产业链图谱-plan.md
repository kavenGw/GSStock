# ASIC 全景产业链图谱 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `SUPPLY_CHAIN_GRAPHS` 字典中新增 `asic` 条目，承载全球（博通系）+ 国产（寒武纪/海光等）算力 ASIC 全景产业链，A 股映射端覆盖封测、FCBGA 基板、AI PCB、CoWoS 设备国产替代、HBM 配套、光模块互联与服务器整机。

**Architecture:** 单文件配置改动（`app/config/supply_chain.py`）+ 一个新契约测试文件。产业链路由按字典遍历自动注册，零路由/模板/seed 改动。tag 默认 `not_analyzed`，已有 buffett 或季报分析的标的留空 tag。

**Tech Stack:** Python dict、pytest、Flask 蓝图测试客户端

**Spec:** `docs/plans/2026-05-09-asic产业链图谱-design.md`

---

## File Structure

| 文件 | 操作 | 说明 |
|------|------|------|
| `app/config/supply_chain.py` | Modify (append) | 在 `SUPPLY_CHAIN_GRAPHS` 字典末尾追加 `'asic': {...}` 条目，约 200 行 |
| `tests/test_supply_chain_asic.py` | Create | 契约测试，参考 `tests/test_supply_chain_intel_hygon.py` 风格 |
| `data/tag_audit_asic.txt` | 临时（不入库）| Task 1 产出的"已分析"标的清单，仅用于本次实施，结束后 `rm` |

---

## Task 1: 已分析文档清单核验（决定 tag 留空名单）

**Files:**
- Read: `docs/analysis/`、`docs/analysis/*/`（含 26q1 等子目录）
- Output: `data/tag_audit_asic.txt`（临时清单，本任务结束后删除）

候选 12 只：002463 沪电、002916 深南、002436 兴森、002156 通富、600584 长电、002185 华天、300308 旭创、300502 新易盛、300394 天孚、002281 光迅、688012 中微、002371 北方华创、601138 富联、300620 光库。

- [ ] **Step 1: 用 Glob 双路扫描（代码 + 中文名）**

按代码扫描（每只一次）：
```bash
ls docs/analysis/ docs/analysis/*/ 2>&1 | grep -E "002463|002916|002436|002156|600584|002185|300308|300502|300394|002281|688012|002371|601138|300620"
```

按中文名扫描：
```bash
ls docs/analysis/ docs/analysis/*/ 2>&1 | grep -E "沪电|深南|兴森|通富|长电|华天|中际|新易盛|天孚|光迅|北方华创|中微公司|工业富联|光库"
```

- [ ] **Step 2: 把命中的代码列入 ANALYZED_CODES，未命中归入 NOT_ANALYZED**

把核验结果以注释形式写入 `data/tag_audit_asic.txt`：
```
# tag 留空（已有 docs/analysis 文档）：
002463  # 沪电股份 - 26Q1 + 泰国工厂 + 近期走势专题
002156  # 通富微电 - 26Q1 + 多个专题
600584  # 长电科技 - 26Q1
002185  # 华天科技 - 26Q1
002281  # 光迅科技 - buffett
300620  # 光库科技 - buffett (与光迅合并)
... (Step 1 实际命中即列)

# tag = not_analyzed：
余下未命中标的
```

- [ ] **Step 3: Commit（仅核验产出，不改代码）**

无需 git commit，本步只是确定后续任务用的 tag 映射数据。`data/tag_audit_asic.txt` 在 `data/` 目录下（已 gitignore 或不入库），任务全部结束后删除。

---

## Task 2: 添加 asic 顶层骨架（core / description / competitors / 三层空 dict）

**Files:**
- Modify: `app/config/supply_chain.py`（在 `SUPPLY_CHAIN_GRAPHS` 字典末尾，最后一个 `}` 之前追加）
- Test: `tests/test_supply_chain_asic.py`（新建）

- [ ] **Step 1: Write the failing test (skeleton structure)**

创建 `tests/test_supply_chain_asic.py`：

```python
"""ASIC 全景产业链图谱契约测试"""
import pytest
from flask import Flask

from app.config.supply_chain import SUPPLY_CHAIN_GRAPHS


def test_asic_top_level_present():
    assert 'asic' in SUPPLY_CHAIN_GRAPHS
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    assert asic['name'] == 'ASIC 算力芯片'
    core = asic['core']
    assert core['code'] == 'AVGO'
    assert core['market'] == 'US'
    assert 'Broadcom' in core['name'] or '博通' in core['name']


def test_asic_three_layers_present():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    for key in ('upstream', 'midstream', 'downstream'):
        assert key in asic, f'缺 {key} 层'


def test_asic_competitors_four_entries():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    competitors = asic.get('competitors', {})
    expected = {'AVGO', 'MRVL', '688256', '688041'}
    assert set(competitors.keys()) == expected, \
        f'competitors 应有 {expected}，实际 {set(competitors.keys())}'
```

- [ ] **Step 2: Run test to verify it fails**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 三个测试均 FAIL（`'asic' in SUPPLY_CHAIN_GRAPHS` 不通过）

- [ ] **Step 3: Add asic skeleton to supply_chain.py**

在 `app/config/supply_chain.py` 的 `SUPPLY_CHAIN_GRAPHS` 字典末尾、最后一个 `}` 之前追加：

```python
    'asic': {
        'name': 'ASIC 算力芯片',
        'core': {
            'name': 'Broadcom 博通',
            'code': 'AVGO',
            'market': 'US',
        },
        'description': (
            'ASIC（专用集成电路）算力芯片产业链，覆盖全球云厂自研 ASIC（Google TPU、'
            'Meta MTIA、AWS Trainium、字节自研）与国产 AI ASIC（寒武纪、海光、华为昇腾、'
            '平头哥、燧原、沐曦）两条主线。设计端由 Broadcom / Marvell / Alchip 主导'
            '全球代工设计，国产以寒武纪 / 海光为核心。中国 A 股映射端聚焦 FCBGA 封装基板、'
            '高多层 AI PCB、CoWoS 设备国产替代、HBM 接口与分销、光模块卡间互联、'
            'AI 服务器整机六大配套环节，不少标的与 nvidia / ascend / cpu 产业链交叠。'
        ),
        'upstream': {},
        'midstream': {},
        'downstream': {},
        'competitors': {
            'AVGO':   {'name': 'Broadcom 博通', 'market': 'US'},
            'MRVL':   {'name': 'Marvell',       'market': 'US'},
            '688256': {'name': '寒武纪',         'market': 'A'},
            '688041': {'name': '海光信息',       'market': 'A'},
        },
    },
```

- [ ] **Step 4: Run test to verify it passes**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 三个测试 PASS

- [ ] **Step 5: Commit**

```bash
git add app/config/supply_chain.py tests/test_supply_chain_asic.py
git commit -m "feat(supply_chain): 添加 asic 图谱骨架（核心 + competitors）"
```

---

## Task 3: 填充 upstream 上游（IP / 代工 / CoWoS 设备 / HBM）

**Files:**
- Modify: `app/config/supply_chain.py`（替换 `'upstream': {}` 为完整字典）
- Test: `tests/test_supply_chain_asic.py`

- [ ] **Step 1: Add the failing test**

向 `tests/test_supply_chain_asic.py` 追加：

```python
def test_asic_upstream_categories():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    upstream = asic['upstream']
    expected_keys = {'design_ip', 'foundry', 'cowos_equipment', 'hbm'}
    assert set(upstream.keys()) == expected_keys, \
        f'upstream 应有 {expected_keys}，实际 {set(upstream.keys())}'

    for cat_key, cat in upstream.items():
        assert 'name' in cat and 'companies' in cat, f'{cat_key} 缺字段'
        for code, info in cat['companies'].items():
            assert info.get('name') and info.get('role'), \
                f'upstream/{cat_key}/{code} 缺 name 或 role'


def test_asic_upstream_key_companies():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    upstream = asic['upstream']
    assert '688521' in upstream['design_ip']['companies'], '芯原应在设计 IP'
    assert '688981' in upstream['foundry']['companies'], '中芯国际应在晶圆代工'
    assert '002371' in upstream['cowos_equipment']['companies'], '北方华创应在 CoWoS 设备'
    assert '688008' in upstream['hbm']['companies'], '澜起科技应在 HBM'
```

- [ ] **Step 2: Run test to verify it fails**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py::test_asic_upstream_categories tests/test_supply_chain_asic.py::test_asic_upstream_key_companies -v`
Expected: 两个新测试 FAIL（upstream 为空）

- [ ] **Step 3: 替换 upstream 为完整字典**

在 `app/config/supply_chain.py` 的 asic 节点中，把 `'upstream': {},` 替换为：

```python
        'upstream': {
            'design_ip': {
                'name': '设计 IP / EDA',
                'companies': {
                    '688521': {'name': '芯原股份', 'role': 'RISC-V/NPU IP，国产 ASIC 设计平台', 'tag': 'not_analyzed'},
                },
            },
            'foundry': {
                'name': '晶圆代工',
                'companies': {
                    '688981': {'name': '中芯国际', 'role': '寒武纪/海光主要代工承接，14nm 已稳定（同属 cpu 产业链）', 'tag': 'not_analyzed'},
                    '688347': {'name': '华虹半导体', 'role': '28nm 特色工艺，承接部分 ASIC', 'tag': 'not_analyzed'},
                },
            },
            'cowos_equipment': {
                'name': 'CoWoS 设备国产替代',
                'companies': {
                    '002371': {'name': '北方华创', 'role': '薄膜沉积/刻蚀（同属 nvidia 产业链）'},
                    '688012': {'name': '中微公司', 'role': '刻蚀设备（同属 nvidia 产业链）'},
                    '688072': {'name': '拓荆科技', 'role': '薄膜沉积，CoWoS 替代关键节点', 'tag': 'not_analyzed'},
                    '688120': {'name': '华海清科', 'role': 'CMP 设备，国产唯一', 'tag': 'not_analyzed'},
                },
            },
            'hbm': {
                'name': 'HBM 配套',
                'companies': {
                    '688008': {'name': '澜起科技', 'role': 'DDR5/HBM 内存接口 RCD/MRCD/MDB', 'tag': 'not_analyzed'},
                    '300475': {'name': '香农芯创', 'role': 'SK 海力士 HBM 国内分销', 'tag': 'not_analyzed'},
                },
            },
        },
```

> 注意：002371 和 688012 已有分析（在 lumentum 图标 frontEC），按 design.md §2.6 留空 tag。Task 1 核验若发现 688521/688981/688347/688072/688120/688008/300475 中有已分析的，对应改为留空 tag。

- [ ] **Step 4: Run test to verify it passes**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 全部 PASS（含原 3 个 + 新 2 个）

- [ ] **Step 5: Commit**

```bash
git add app/config/supply_chain.py tests/test_supply_chain_asic.py
git commit -m "feat(supply_chain): asic 图谱补充 upstream 层（IP/代工/CoWoS/HBM）"
```

---

## Task 4: 填充 midstream 中游（设计公司 / 封测 / FCBGA）

**Files:**
- Modify: `app/config/supply_chain.py`
- Test: `tests/test_supply_chain_asic.py`

- [ ] **Step 1: Add the failing test**

追加：

```python
def test_asic_midstream_categories():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    midstream = asic['midstream']
    expected_keys = {'global_design', 'domestic_design', 'packaging', 'fcbga_substrate'}
    assert set(midstream.keys()) == expected_keys, \
        f'midstream 应有 {expected_keys}，实际 {set(midstream.keys())}'


def test_asic_midstream_key_companies():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    mid = asic['midstream']
    assert 'AVGO' in mid['global_design']['companies'], 'Broadcom 应在全球设计层'
    assert 'MRVL' in mid['global_design']['companies'], 'Marvell 应在全球设计层'
    assert '688256' in mid['domestic_design']['companies'], '寒武纪应在国产设计层'
    assert '688041' in mid['domestic_design']['companies'], '海光应在国产设计层'
    assert '002156' in mid['packaging']['companies'], '通富微电应在封测层'
    assert '002916' in mid['fcbga_substrate']['companies'], '深南电路应在 FCBGA 层'


def test_asic_midstream_global_design_us_market():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    avgo = asic['midstream']['global_design']['companies']['AVGO']
    assert avgo.get('market') == 'US', '全球设计 AVGO 必须标 market=US'
    mrvl = asic['midstream']['global_design']['companies']['MRVL']
    assert mrvl.get('market') == 'US'
```

- [ ] **Step 2: Run test to verify it fails**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 三个新测试 FAIL

- [ ] **Step 3: 替换 midstream 为完整字典**

把 `'midstream': {},` 替换为：

```python
        'midstream': {
            'global_design': {
                'name': '全球 ASIC 设计（云厂自研主力）',
                'description': 'Google TPU、Meta MTIA、AWS Trainium、字节自研 ASIC 主要由 Broadcom / Marvell / Alchip 代工设计。Alchip（世芯，台股）未上市 A 股，仅文本说明。',
                'companies': {
                    'AVGO': {'name': 'Broadcom 博通', 'market': 'US', 'role': '全球 ASIC 代工设计龙头：Google TPU / Meta MTIA / 字节'},
                    'MRVL': {'name': 'Marvell',       'market': 'US', 'role': 'AWS Trainium / Inferentia 设计代工'},
                },
            },
            'domestic_design': {
                'name': '国产 AI ASIC 设计',
                'description': '寒武纪、海光为上市主力。华为昇腾深化链路见 ascend 图谱。平头哥（阿里含光）、燧原（邃思）、沐曦（曦云）均未上市，仅文本提及。',
                'companies': {
                    '688256': {'name': '寒武纪',   'role': '国产通用 AI ASIC 设计龙头，思元 590 量产', 'tag': 'not_analyzed'},
                    '688041': {'name': '海光信息', 'role': 'DCU 加速卡（GPGPU 形态 ASIC），深算二号量产（同属 cpu 产业链）', 'tag': 'not_analyzed'},
                },
            },
            'packaging': {
                'name': '封测',
                'companies': {
                    '002156': {'name': '通富微电', 'role': 'AMD 合资 TFAMD，海光/寒武纪封测主力（同属 cpu/nvidia 产业链）'},
                    '600584': {'name': '长电科技', 'role': 'FCBGA/Chiplet/CoWoS-S 替代封装（同属 cpu/nvidia 产业链）'},
                    '002185': {'name': '华天科技', 'role': '国产 ASIC/SoC 封测（同属 cpu 产业链）'},
                    '688362': {'name': '甬矽电子', 'role': '中高端封测，承接寒武纪部分订单', 'tag': 'not_analyzed'},
                },
            },
            'fcbga_substrate': {
                'name': 'FCBGA 封装基板',
                'companies': {
                    '002916': {'name': '深南电路', 'role': 'FCBGA 国产龙头，AI ASIC 大尺寸高层数（同属 cpu 产业链）'},
                    '002436': {'name': '兴森科技', 'role': 'FCBGA 小批量供货北美 ASIC 客户（同属 cpu 产业链）'},
                },
            },
        },
```

> 002156、600584、002185、002916、002436 按 Task 1 核验留空 tag（已有季报点评 / 专题）。

- [ ] **Step 4: Run test to verify it passes**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add app/config/supply_chain.py tests/test_supply_chain_asic.py
git commit -m "feat(supply_chain): asic 图谱补充 midstream 层（全球/国产设计 + 封测 + FCBGA）"
```

---

## Task 5: 填充 downstream 下游（服务器 / PCB / 光模块 / 散热）

**Files:**
- Modify: `app/config/supply_chain.py`
- Test: `tests/test_supply_chain_asic.py`

- [ ] **Step 1: Add the failing test**

追加：

```python
def test_asic_downstream_categories():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    down = asic['downstream']
    expected_keys = {'server_odm', 'ai_pcb', 'optical_module', 'cooling_power'}
    assert set(down.keys()) == expected_keys


def test_asic_downstream_key_companies():
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    down = asic['downstream']
    assert '601138' in down['server_odm']['companies'], '富联应在服务器 ODM'
    assert '000977' in down['server_odm']['companies'], '浪潮信息应在服务器 ODM'
    assert '603019' in down['server_odm']['companies'], '中科曙光应在服务器 ODM'
    assert '002463' in down['ai_pcb']['companies'], '沪电应在 AI PCB'
    assert '300308' in down['optical_module']['companies'], '旭创应在光模块'
    assert '002837' in down['cooling_power']['companies'], '英维克应在散热'


def test_asic_role_text_includes_cross_chain_marks():
    """跨产业链标的 role 末尾应含「同属 X 产业链」反向引用"""
    asic = SUPPLY_CHAIN_GRAPHS['asic']
    fulian = asic['downstream']['server_odm']['companies']['601138']
    assert '同属' in fulian['role'], '工业富联 role 应含跨链反向引用'
    huadian = asic['downstream']['ai_pcb']['companies']['002463']
    assert '同属 nvidia' in huadian['role'], '沪电股份应注明同属 nvidia'
```

- [ ] **Step 2: Run test to verify it fails**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 三个新测试 FAIL

- [ ] **Step 3: 替换 downstream 为完整字典**

把 `'downstream': {},` 替换为：

```python
        'downstream': {
            'server_odm': {
                'name': '服务器整机 ODM',
                'companies': {
                    '601138': {'name': '工业富联', 'role': '全球最大 AI 服务器代工，TPU/Trainium 整机集成（同属 cpu/nvidia/ascend 产业链）'},
                    '000977': {'name': '浪潮信息', 'role': '国产 AI 服务器龙头，海光/寒武纪整机（同属 cpu 产业链）', 'tag': 'not_analyzed'},
                    '603019': {'name': '中科曙光', 'role': '海光大股东，DCU 整机捆绑', 'tag': 'not_analyzed'},
                },
            },
            'ai_pcb': {
                'name': 'AI PCB',
                'companies': {
                    '002463': {'name': '沪电股份', 'role': '800G/1.6T 板卡 PCB（同属 nvidia 产业链）'},
                    '300476': {'name': '胜宏科技', 'role': '高多层 AI PCB', 'tag': 'not_analyzed'},
                    '688183': {'name': '生益电子', 'role': 'AI 服务器 PCB', 'tag': 'not_analyzed'},
                },
            },
            'optical_module': {
                'name': '光模块卡间互联',
                'companies': {
                    '300308': {'name': '中际旭创', 'role': '800G/1.6T 光模块（同属 lumentum/nvidia 产业链）'},
                    '300502': {'name': '新易盛',   'role': '400G/800G 光模块（同属 lumentum 产业链）'},
                    '300394': {'name': '天孚通信', 'role': '光引擎/连接器（同属 lumentum 产业链）'},
                    '002281': {'name': '光迅科技', 'role': '光芯片 EML/DFB（同属 lumentum 产业链）'},
                },
            },
            'cooling_power': {
                'name': '散热与电源',
                'companies': {
                    '002837': {'name': '英维克',   'role': '液冷温控龙头', 'tag': 'not_analyzed'},
                    '300870': {'name': '欧陆通',   'role': 'AI 服务器电源', 'tag': 'not_analyzed'},
                },
            },
        },
```

> 601138、002463、300308、300502、300394、002281 按 Task 1 核验留空 tag。

- [ ] **Step 4: Run test to verify it passes**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add app/config/supply_chain.py tests/test_supply_chain_asic.py
git commit -m "feat(supply_chain): asic 图谱补充 downstream 层（服务器/PCB/光模块/散热）"
```

---

## Task 6: 路由 JSON 契约测试（确保 /supply-chain/api/asic 可用）

**Files:**
- Test: `tests/test_supply_chain_asic.py`

- [ ] **Step 1: Add the failing test**

追加：

```python
@pytest.fixture
def client():
    """轻量路由测试：跳过 create_app（API 端点不渲染模板）"""
    from app.routes import supply_chain_bp
    app = Flask(__name__)
    app.register_blueprint(supply_chain_bp, url_prefix='/supply-chain')
    return app.test_client()


def test_asic_api_returns_json(client):
    resp = client.get('/supply-chain/api/asic')
    assert resp.status_code == 200, f'路由返回 {resp.status_code}'
    data = resp.get_json()
    assert data is not None, 'API 未返回 JSON'


def test_asic_api_contains_core_node(client):
    resp = client.get('/supply-chain/api/asic')
    data = resp.get_json()
    nodes = data.get('nodes', [])
    core_nodes = [n for n in nodes if n.get('category') == 'core']
    assert len(core_nodes) >= 1, 'core 节点必须存在'
    assert any('AVGO' in n.get('name', '') or 'Broadcom' in n.get('name', '') or '博通' in n.get('name', '')
               for n in core_nodes), 'core 节点应是 Broadcom AVGO'


def test_asic_api_contains_competitors(client):
    resp = client.get('/supply-chain/api/asic')
    data = resp.get_json()
    nodes = data.get('nodes', [])
    node_names = ' '.join(n.get('name', '') for n in nodes)
    for keyword in ('寒武纪', '海光', 'Marvell'):
        assert keyword in node_names, f'competitor {keyword} 未出现在节点中'
```

- [ ] **Step 2: Run test to verify it passes (full suite)**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_asic.py -v`
Expected: 全部 PASS（应有 11+ 条测试）

> 若 test_asic_api_returns_json FAIL（404）：说明 `app/routes/supply_chain.py` 没有按字典遍历自动注册路由，需检查路由定义。design.md §4 已假定路由按 dict 遍历自动注册，若假定不成立此处暴露问题。

- [ ] **Step 3: 同时跑既有 supply_chain 测试确保未回归**

Run: `PYTHONIOENCODING=utf-8 SCHEDULER_ENABLED=0 python -m pytest tests/test_supply_chain_intel_hygon.py tests/test_supply_chain_asic.py -v`
Expected: 全部 PASS

- [ ] **Step 4: Commit**

```bash
git add tests/test_supply_chain_asic.py
git commit -m "test(supply_chain): asic 图谱路由 JSON 契约测试"
```

---

## Task 7: 浏览器手工验证 + 清理临时文件

**Files:**
- Cleanup: `data/tag_audit_asic.txt`（删除）

- [ ] **Step 1: 启动应用**

```bash
python run.py
```

后台启动，等待 "Running on http://127.0.0.1:5000" 出现。

- [ ] **Step 2: 浏览器访问**

打开 `http://127.0.0.1:5000/supply-chain/?graph=asic`，验证：
- 三层结构（upstream/midstream/downstream）渲染
- core 节点显示「Broadcom 博通 AVGO」
- competitors 区显示 4 个标的（AVGO/MRVL/688256/688041）
- 任选一只重叠标的（如 002916 深南电路）点击跳转个股页正常
- 任选一只新引入标的（如 688362 甬矽电子）确认不报错（前端无价格也允许）

打开 `http://127.0.0.1:5000/supply-chain/api/asic`，验证 JSON 含 nodes / links。

- [ ] **Step 3: 验证既有图谱未受影响**

抽查 `http://127.0.0.1:5000/supply-chain/?graph=ascend`、`?graph=cpu`、`?graph=nvidia` 仍正常渲染（asic 图谱仅追加，不应回归）。

- [ ] **Step 4: 清理临时文件**

```bash
rm data/tag_audit_asic.txt
```

- [ ] **Step 5: 终止应用**

终止后台 `python run.py` 进程。

- [ ] **Step 6: 最终核验**

```bash
git status
git log --oneline -7
```

Expected:
- working tree clean（无未提交变动）
- 最近 5 次 commit 是 Task 2-6 的 commit + 设计稿 commit

---

## Self-Review

**Spec coverage check:**
- ✅ design §2.1 顶层结构 → Task 2
- ✅ design §2.3 upstream 4 子类 → Task 3
- ✅ design §2.4 midstream 4 子类 → Task 4
- ✅ design §2.5 downstream 4 子类 → Task 5
- ✅ design §2.6 tag 策略 → Task 1（核验）+ Task 3-5（应用）
- ✅ design §3 同属链反向引用 → Task 5 测试覆盖
- ✅ design §5 验证清单 → Task 7

**Placeholder scan:** 无 TBD/TODO，每个 Step 包含完整代码或确切命令。

**Type consistency:** key 命名一致（design_ip / foundry / cowos_equipment / hbm / global_design / domestic_design / packaging / fcbga_substrate / server_odm / ai_pcb / optical_module / cooling_power）。所有 companies 字典 value 字段统一为 `name` / `role` / 可选 `market` / 可选 `tag`。

**Risks:**
- Task 6 路由测试若 404，说明动态注册假设不成立 → 该 Task 第 2 步会暴露此问题，需读 `app/routes/supply_chain.py` 后修补
- 新引入的 9 只代码（688521/688347/688072/688120/300475/688362/300476/688183/002837/300870）未在 `Stock` 表中时，前端实时价显示空 → 不阻塞图谱本身渲染，是后续可观察的次要问题
